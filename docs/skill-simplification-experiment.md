# 建模 skill 减负实验

在 `codex/skill-simplification` 独立 worktree 中测试。原工作区的未提交源码先快照为 `cfc9a52`，原工作区未改动；该提交是用户现状基线，不应视为本次功能改动。

**前三轮结论：没有证据证明精简入口提高了建模质量，因此恢复了原入口。** 当时八次探索运行都在各自 20 分钟截止时未完成最终回复。保留有独立回归证据的接口纠错、绝对公差三角化和真实表面壁厚采样。短入口版本留在 `b6ef315`，不作为新的默认推荐；后续 BuildSession 对照在本文第 4 轮单独记录。

## 已确认的问题与处理

### 入口与工具协议

实验中把 `SKILL.md` 从 196 行、10,942 字节减为 93 行、5,382 字节。入口保留目标理解、构建、看图、修复和交付；多零件、安装、紧固、颜色等按当前设计需要读取已有指导。没有新增一组互相竞争的 skill。第 3 轮结束后恢复原文件，实验快照保持不变。

仅缩短入口的首轮实验没有证明建模变好：两组外壳任务都在 20 分钟超时。原版第一次编译约在第 744 秒，精简版仍停留在 intent 准备阶段。不能以文档更短或规则检查通过代替建模效果。

随后把 `write_intent` 的枚举、必填字段、部件/特征引用规则放到它自己的 capability 查询结果中，并让错误直接列出合法值。枚举与模式来自验证常量，手写的结构约束通过回归测试核对；验证规则没有放宽。未知尺寸通过显式范围表达，用户要求保持固定。这减少了在入口文档堆积 schema 细节的必要，但实际收益仍取决于作者是否查询接口。

### 曲面导出的毫米公差没有按毫米执行

现有代码把 `linear_tolerance_mm` 传给 build123d 的三角化方法；所装版本却使用相对偏差。杯子实验中，模型为解决 STL 审计错误放弃了更平滑的椭圆放样。

相同尺度的空心椭圆放样最小复现中，最大边界误差由 **0.099787 mm** 降至 **0.006484 mm**，通过原有 **0.05 mm** 审计阈值。修复复用仓库另一分支已有的绝对三角化 helper，独立复制 BRep 后网格化，避免复用或改写源缓存；未引入该分支的其他 authoring 架构。

这项精度修复增加三角面和导出时间：该复现约由 0.20 秒增至 2.44 秒。本次椭圆直柱对照不触发旧错误；最终经过反复修改的两个杯子 STEP 重新走旧导出路径，也都能通过 STEP/STL 几何一致性审计。这不代表全部建模验收通过。最小复现、历史失败和最终工件的结果不可混为一谈。

证据：[三角化复现](../workspace/skill-ab-v2/inspection/TESSELLATION.zh-CN.md)、[原始数值](../workspace/skill-ab-v2/inspection/tessellation-measurements.json)、[最终 STEP 独立读回](../workspace/skill-ab-v2/inspection/cup-step-readback.json)。

### 壁厚采样把自己的表面当成对面

旧检测把近似共面的三角形分组，再取该组面心的平均位置。这一点可能不在真实网格表面上。向内发射的射线随即碰到自己的表面，把很小的回射距离报成壁厚。

单独用 trimesh 构造的 20 × 20 × 10 mm 实心块，仅将顶角下移 0.001 mm，仍然封闭，垂直材料跨度为 9.999–10 mm，却得到 **0.00017 mm** 的最小值和 P05、约 **25%** 的采样区域面积权重被误判。这证明误报不依赖 STEP 或上面的三角化错误。

修复后保留确实落在真实三角面上的平面中心；其余使用真实三角面中心和对应法向，补回旧算法遗漏的未分组面。原有射线容差、P05、物理阈值和采样预算均未改变。箱体反例恢复到 **9.9995 mm**。直接把所有平面中心换成三角面中心曾改变薄板的估计值，原有回归及时检出，该做法没有进入最终补丁。

对同一份固定 STL（未重新三角化），两组杯子的 P05 分别从 0.00025/0.00023 mm 恢复至 **3.65972/3.12797 mm**。仍各有 2,048 个有效样本，代表区域的面积覆盖约 73.53%/69.23%。局部最小估计 **0.10982/0.09588 mm** 仍然保留，风险集中在杯口附近，不再遍布整个杯身。它们是 `max_sphere` 的局部估计，不能等同于截面法向壁厚，也不能据此宣称整个杯子满足 2 mm 要求。

证据：[同一 STL 对照](../workspace/skill-ab-v2/inspection/thickness-fix-comparison.json)、[箱体对照](../workspace/skill-ab-v2/inspection/dented-box-fixed-comparison.json)、[复现脚本](../workspace/skill-ab-v2/inspection/reproduce-thickness-artifact.py)。杯子对照记录输入和检测代码的 hash，箱体记录构造与数值；违规面积比明确针对抽样代表的区域。厚度与导出实验使用不同迭代阶段的独立快照，各仅作同一输入前后对照，不将两组测量拼成一个模型的验收结论。

## 真实任务测试设计

使用项目现有 `CodexRuntime`、SDK 和托管 Python 依赖，没有安装包。两臂使用独立复制的 `bin`/`skills`、全新 session、相同 prompt、`openai/gpt-5.6-sol`、medium reasoning 和 20 分钟硬截止。凭据仅在内存使用，不复制到实验目录；事件输出脱敏。

任务包括带参考图的 95 mm 高空心曲面杯，以及含显示屏/PCB 安装、线缆通道和可拆后盖的 100 × 40 × 70 mm 两件外壳。验收来自原始 brief，不以模型自己填写的 intent 代替用户目标。杯子参考图是可复现的技术示意图，不是产品照片。

| 轮次 | 对比内容 | 任务 | 观察 |
|---|---|---|---|
| 1 | 原版 vs 仅精简入口 | 外壳各 1 次 | 均超时；精简入口未证明改善 |
| 2 | 原版 vs 精简入口 + capability/错误信息 | 杯子各 1 次 | 均超时；分别编译 10/3 次；揭示上述曲面导出和壁厚误报 |
| 3 | 两组共用修复后的工具，仅比较入口与接口提示 | 杯子、外壳各 1 对 | 冻结于 `ba298ec`；四次均超时，未证明精简方案提升质量 |

第 2 轮首次完整编译返回预览路径的时间约为原版第 419 秒、候选第 475 秒；这不等于已经完成看图。候选最后一次完整编译 `pass:true`，但超时且没有最终回复，不计为任务完成。编译器始终返回 `deliveryReady:false`，看图后也不会更新这个字段，它不是任务失败判据。超时中断的最新报告可能覆盖先前成功状态，历史工件以保存的 hash 和事件匹配。

第 2 轮原定的后续重复运行在定位确定性工具错误后暂停，改为修复后复测；不同候选版本不合并统计。样本少、无模型随机种子，也不是显著性实验。SDK 没有完整暴露原生看图事件；不能据此声称模型没有看图。独立 STEP/STL 检查和人工看预览仅覆盖记录的几何及有限采样，不证明任意位置的最小壁厚。

原始脚本、prompt、模型设置、哈希与逐步事件分别位于 [第 1 轮](../workspace/skill-ab/)、[第 2 轮](../workspace/skill-ab-v2/)、[第 3 轮](../workspace/skill-ab-v3/)；这些目录被 workspace 忽略规则排除，不进入产品发布内容。前两轮每轮两任务并行，第 3 轮四任务并行；跨轮用时还受到并发负载和模型波动影响，不能归因于单一补丁。

第 3 轮的实际安排以 `freeze.json`、各次 `plan.json`/`status.json` 和 [执行备注](../workspace/skill-ab-v3/execution-notes.md) 为准。复制的 `cases/rubric.json` 保留初始计划元数据，只有其几何要求用于本轮验收；没有进行盲评或统计显著性检验。

## 第 3 轮实际结果

| 任务 | 入口 | 首次编译命令 | 编译尝试 | 结果 |
|---|---|---:|---:|---|
| 杯子 | 原版 | 475.7 秒 | 4 | 最后自动编译通过；超时，无最终回复 |
| 杯子 | 精简 + 接口提示 | 520.9 秒 | 4 | 最后编译仍有 QA 错误；超时 |
| 外壳 | 原版 | 未进入 | 0 | 没有建模源或 STEP；超时 |
| 外壳 | 精简 + 接口提示 | 846.5 秒 | 6 | 最后一次在 1197.4 秒启动，未完成；超时 |

候选杯子最后完成编译的几何存在真实薄壁：独立 BRep 法向材料跨度在收脚处测得约 **0.999 mm**，840 条测试射线中有 45 条小于 2 mm，并核对了射线退出前后的材料/空域状态。这与杯口 `max_sphere` 的小数值是不同问题。末尾源文件把壁厚参数改为 4 mm，但没有重新编译，不能把该修改视为已修好实物。两个中间杯子快照均有真实敞口内腔和足够底厚；圆角使 z=95 顶端外宽降为约 52.45/52.13 mm，z=94.5 则约为 53.93/53.61 mm，需按明确测量高度评估 54 mm 的顶部要求。候选上口后移约 2.49 mm，弱于参考约 5 mm 的关系。

原版曾为杯口局部告警把外壳改成 ruled loft，看图后发现环状折痕并撤回；最终外壳恢复 smooth，仅内腔保留 ruled。这个过程说明视觉复核有实际价值，也说明告警解释及合同修订仍会耗费建模时间。

最终原版杯子 839 条有效 BRep 法向射线（另 1 条方向不明确跳过）最小跨度为 3.5 mm，未采到低于 2 mm；这仍不是全局最薄位置的证明。其 QA 保留 `max_sphere` 局部警告（最小 0.0043 mm、P05 为 3.5 mm），不能采信模型曾声称的“告警已消除”。最终 z=95/z=94.99/z=94.5 的外宽分别为 **53.017/53.216/54.015 mm**。候选最终 STL 与先前薄壁快照字节相同，所述 0.999 mm 问题仍在；最后阻塞还包括 stable-foot 缺少物理观察记录。

候选外壳最后可测的失败工件宽 **115.6 mm**，源中把最小角坐标用于中心对齐的 Box；现有尺寸检查正确拦截了错误。STEP 独立布尔测量还发现屏幕与壳体相交 **121.6 mm³**、PCB 相交 **144 mm³**、后盖与壳体相交 **226.43 mm³**，所以只修宽度仍不足以证明可装配。54×28 前窗和 12×6 底部通孔确实贯通。截止前源文件又修正了对齐方式，但未生成匹配的新报告和实物；最后日志出现 `housing-body must be watertight`，不能据此把旧实物当成新源码的结果。

工件索引：[杯子中间测量](../workspace/skill-ab-v3/inspection/cup-intermediate-review.json)、[杯子最终状态与测量](../workspace/skill-ab-v3/inspection/cup-final-review.json)、[外壳独立 STEP 测量](../workspace/skill-ab-v3/inspection/enclosure-candidate-b680376d-1c0f-4cdf-8dc2-526f272272bb/independent-measurements.json)、[外壳最终状态与未验证源码](../workspace/skill-ab-v3/inspection/enclosure-final-state.json)。

## 代码与验证

功能提交按职责拆开：

- `b6ef315`：精简入口。
- `f939efb`：writer capability 约束与可修复错误。
- `4f5bd9f`：绝对毫米三角化与源缓存隔离。
- `ba298ec`：真实表面壁厚采样、遗漏面覆盖和采样范围说明。
- `8f30648`：恢复原 `SKILL.md`，实验版本与记录保留。

相关测试按文件记录如下，均已通过；这是本次各阶段的验证记录，不是全仓测试声明：

| 测试文件 | 通过数 | 覆盖 |
|---|---:|---|
| `tests/a3d-cli.test.ts` | 12 | CLI 公共入口 |
| `test_skill_shared_files.py` | 4 | skill 引用及共享路径 |
| `test_capability_manifest.py` | 11 | 查询字段、常量一致性、无内核导入 |
| `test_text_a3d_authoring.py` | 17 | writer 约束与错误后修正 |
| `test_intent_revisions.py` | 14 | 已有目标和 revision 不变量 |
| `test_brep_tessellation.py` | 5 | 实际曲面导出精度、源 BRep/缓存不变 |
| `test_brep_stl_normalization.py` | 3 | STL 归一化 |
| `test_display_components.py` | 6 | 展示几何 |
| `test_print_orientation.py` | 2 | 打印朝向 |
| `test_text_a3d_printability.py` | 36 | 箱体误报、未分组面、预算、真实薄板和局部薄壁 |
| `test_authoring_example.py` | 2 | 真实公共示例编译和 STEP/STL 几何 |
| `test_cad_compile.py` | 39 | 编译、QA、超时、报告生命周期 |

共 151 项相关检查。`git diff --check` 通过。skill-creator 的 `quick_validate.py` 因托管环境缺少 PyYAML 无法执行；没有为此安装包，使用现有结构检查及实际运行验证。

入口重构与确定性工具修复可以分别评审、分别取用；不需要整体接入另一套建模框架。

## 后续重构的边界

继续试验应针对重复声明：同一几何目前需在构造、scene 节点和 observe 证据中分别登记；缺一层就触发修复。把几何操作集中到一次定义并派生 scene/证据，比继续扩写入口更值得验证。不可变约束应明确代表用户要求；仅用于实现的截面、内缩量等参数应与用户目标分开，避免每次合理修形都创建合同修订。

第一阶段只读审查了仓库已有 `codex/single-definition-authoring`：其中 320 行 `BuildSession` 能减少重复绑定，但仍依赖预先写 intent，返回的 part 副本也不能自动提交后续圆角。整个分支还大幅调整编译器并删除现有 revision 逻辑，不适合直接合入。因此后续试验限定为几何/证据接口的小步修改，保留当前编译、安装及需求检查。

## 第 4 轮：减少重复登记

基线为 `56a0ced` 的完整版本，保留前面两项数值修复、原版 SKILL 和 capability 提示。候选只增加可选的 `BuildSession` 适配器、隔离原 helper 的证据状态，并更新发现入口和四个示例。没有引入其他分支的编译器或 revision 重构，也没有改变 QA 门限。

`add`/`cut` 从 intent 取得 feature 的零件归属，用同一份实际 BRep 派生操作证据和 scene；`finish` 明确提交圆角等最终修改，避免修改副本却导出旧实体。实现性的圆角操作 ID 不必再成为独立 intent 要求。自攻螺丝节点从同一 interface 的 feature ID 派生，仍进入现有轴线、配合和实际几何检查。位于已有厚实材料内的螺柱通过实体观察登记，不伪称增料成功。

曲面示例把截面数量和内缩量留在 build source；最终外尺寸、开放内腔、最低壁厚及 3 mm 底面仍是固定验收。真实回归保持同一 intent SHA，仅将内缩量 3 改为 4，再编译读回 STEP/STL，确认体积增加、实际壁面改变、外包络及底面不变，source/scene/report/工件绑定均更新。

候选冻结前通过 165 项相关检查：BuildSession 14、capability 12、共享文件 4、authoring 17、source diagnostics 3、compiler 39、颜色 guardrails 27、装配朝向 1、公开示例 4、安装及接口相关 44。公开示例包含两次曲面编译和完整安装案例，不以 mock 代替几何验证。这不是全仓测试声明。

已知边界：`capture` 回滚会话内存，不撤销块内已经成功导出的文件；同进程捕获编译态错误再恢复时，运行级旧诊断文件仍可能影响后续普通异常的归因。正常成功退出不受后一问题影响。编译器的失败状态及文件绑定检查继续保留，本轮未扩大为诊断生命周期重构。

真实模型评估使用 [v4 脚本](../workspace/skill-ab-v4/README.md)，先完成杯子对照及审查，再完成外壳对照；每个任务两臂各一次、每次 20 分钟上限，最多两次运行并行，共四次。skill、runtime、brief、参考图和候选差异分别冻结并记录哈希。代码与示例变短只证明登记代码减少，不能代替模型质量结果。

候选冻结在 `995ec77`。四份建模示例合计由 279 行降至 205 行（减少 74 行）；主 SKILL 从 196 行变为 199 行，适配器为 283 行。复杂的几何判断并未因此消失，不能把这些数字当成建模质量提升。

### 杯子对照结果

| 指标 | 基线 | BuildSession 候选 |
|---|---:|---:|
| 首次写建模源 | 489.5 秒 | 315.6 秒 |
| 首次返回预览路径 | 632.6 秒 | 410.2 秒 |
| 首次自动编译通过 | 1078.0 秒 | 629.6 秒 |
| 编译尝试 | 4 | 4 |
| 20 分钟内最终回复 | 无，超时 | 无，超时 |

两组首次编译前均完成 8 条文档/能力/帮助读取命令，返回约 30,682 / 30,237 个 Unicode 字符（不是 token）。这些准备工作没有明显减少。首次写源和预览的时间差来自这一对探索样本，不能排除模型随机性，也不能全部归因于适配器。

登记错误有实际差别：基线补了 scene 节点却漏了 `observe`，同一特征覆盖错误持续两次编译；候选使用了 `add/cut/export`，但直接给返回副本做圆角，忘记 `finish`，首编译因缺少杯口证据失败。候选补读 3 次能力/示例、9,227 字符后修正提交方式。首个失败返回到下一次通过相隔 219.4 秒，其中也包含看图、加厚内腔及编译渲染，不能全计为接口修复耗时。

两臂后续都主要追逐局部薄壁警告，未修改已生成的 intent。基线最终内缩量 5、口圆角 0.6；候选内缩量 6、口圆角 0.5。它们都没有改变最初的外部截面布置。基线末尾源码与工件匹配；候选在第 4 次完成编译后，把圆角 callback 改成 identity callback，没有真的实现其消息中所说的“短台阶”，也没有重新编译。已从操作记录恢复与第 4 次报告精确匹配的源码，恢复 SHA 为 `6d185d5533a111445939bfe918f8890041fb4cbc30d6f8f8ffe5487a2d026d03`；原快照的源码缺口和未编译版本继续保留。

独立实体测量及实际看图没有证明候选外形更好：

- 最终 XYZ：基线 **82.0000×62.1204×95.0000**，候选 **82.1249×62.3160×95.0000 mm**；均为一个有效 BRep 实体，STL 封闭、方向一致。
- 精确底面外宽均为 **50 mm**，中心及附近 5 条竖线均显示 **3.5 mm** 腔底，有限水平截面均有内孔。基线首版的肩部异常在最终这些截面及预览中已不再出现；有限检查不是全局无孔证明。
- 圆角改变了实际口缘：在 **z=95 / 94.99 / 94.5 mm**，基线外宽为 **53.0455 / 53.2631 / 54.2283**，候选为 **52.3227 / 52.5214 / 53.3213 mm**。不能用源码端截面的 54 代替最终口宽，也未发明额外验收公差。
- 上口相对脚部的 +Y 偏移约 **2.473 / 4.001 mm**。候选的后移更接近参考关系，但底脚保留了明显的外翻薄楔；大面形状并未因接口简化而自动改善。
- 基线 840 条有效 BRep 法向材料射线最小 **2.1416 mm**，没有采到小于 2 的位置。候选有两处脚边短材料跨度，最小 **0.6813 mm**；独立进入/退出检查确认这是外脚边先内收再外鼓形成的局部薄楔，**不是中心腔底只有 0.68 mm**。候选 body/rim 样本最小分别约 **4.9249 / 2.3550 mm**。有限采样不证明全局最小壁厚。

最终 QA 的 `max_sphere` 最小估计为基线 **0.00614**、候选 **0.00446 mm**，与上述真实材料射线不是同一量；不能仅凭这组有限射线断言所有告警都是误报，也不能通过无限加大内缩量替代对具体位置的判断。

证据：[执行流程与事件索引](../workspace/skill-ab-v4/inspection/cup-process-final.md)、[数值与快照索引](../workspace/skill-ab-v4/inspection/cup-numeric-comparison.json)、[恢复源码及来源](../workspace/skill-ab-v4/inspection/recovered-source/candidate-compiled4-recovery.json)、[安装案例 STEP 等价测量](../workspace/skill-ab-v4/inspection/installed-module-equivalence-r2/comparison.json)。

### 外壳对照结果

两臂都在 20 分钟截止时没有写出 Python 建模源码、验证 intent 或启动编译，也没有最终回复。最终库存确认均无 STEP/STP/STL 和编译报告。因此本轮外壳**没有可比较的几何结果**，不能把超时写成编译器失败，也不能把建模计划当成已完成装配。

基线读取 7 次文档/能力，共 79,625 个 Unicode 字符；候选读取 9 次，共 68,961 字符。字符数不是 token。最后一次查询分别在第 609.4 / 752.5 秒返回，之后直到截止仍未写源。候选读过新接口及安装示例，但未生成代码，实际采用情况未知。可观察命令均未失败，这些数据不足以确定停滞的单一原因。

两臂还为尺寸和安装要求明确的外壳做了外观参考准备；基线取得无关图片后弃用，候选报告缺少原生图片检索能力。这说明按“消费外壳”品类直接触发外观指导会带来无关准备，但不能据此断言它独自造成全部超时。

证据：[流程及完整事件索引](../workspace/skill-ab-v4/inspection/enclosure-process-final.md)、[最终工件库存与未测项目](../workspace/skill-ab-v4/inspection/enclosure-artifact-final/no-artifacts.json)。未生成外壳的开口、干涉和装入路径均标为未测；没有事后代写模型来补齐该轮结果。

### 对照之后的两处说明修正

杯子候选最初已经查询 `BuildSession`、`add`、`cut`、`export`，但它们的短说明没有直接写明返回副本如何提交。现将这几条已有 docstring 的首段改为明确指向 `finish()`，让能力查询在调用前暴露关键语义；不增加 SKILL 通用规则，不改变签名或几何行为。12 项 capability 检查通过。此修改晚于 `995ec77` 冻结，**没有进入第 4 轮模型对照**，其模型行为效果尚未验证。

入口原本按“消费外壳或其他外观主导对象”触发 `surface-design.md`；现改为用户要求优先考虑外形、风格或参考复现时才触发。保留真实外观任务的参考指导及现有视觉检查，没有增加规则条目。4 项共享文件检查通过。这项修正同样发生在冻结对照之后，尚未验证模型行为改善。

### 本轮结论与取舍

自动派生 scene/证据在代码层面减少了重复声明，真实杯子也避免了基线的一类漏登记错误；但返回副本的提交语义又引入新返工。候选首次预览早约 3 分 42 秒，只是一对探索样本的观察。四次均未完成交付，候选杯子保留真实薄楔，外壳没有生成几何，**不支持把适配器推广为已验证的质量优化**。

因此将适配器保留为实验分支中的可选接口，未合并到原工作区。165 项检查证明相关代码及例子的回归情况，不证明模型会更好地设计。示例少了 74 行，同时增加了 283 行适配器及维护成本；不能宣称系统整体变小。

这轮支持的拆分边界是：用户尺寸和使用要求留在 intent，截面布置及内缩量留在建模源，机械登记由接口派生，实际形状和装配结果独立验收。外观指导按任务需要加载，接口易错点就近写在能力说明中。现有证据不足以支持继续增加通用规则或引入整套新框架；确定性工具修复、接口试验和本轮两处说明修正保持独立提交，可分别评审和回退。

## 第 5 轮：几何反馈与可检查的草模

本轮实现四项此前提出的优化，并将单元/实体回归与模型行为对照分开。基线为上一轮结束时的 `46c8d77`，候选冻结在 `acbb92d`；两臂已有 BuildSession、先前数值修复及接口说明修正。实际变量为 11 个 bin/skill 文件，runtime 和 AGENTS 保持相同。

### 已实现并验证的工具行为

1. **薄点定位进入紧凑输出。** 旧摘要优先数字并截断嵌套字段，已有最低点坐标因此被丢掉。现在优先返回 `witness`：最低估计、网格/打印坐标、绑定可用时逆变换得到的 `semanticPointMm`、测量方法。包围盒相交的 feature 仍只称候选，不作为根因证明。没有改变测量算法、射线、门限或 12,000 字符预算。
2. **比较同类 warning 的实测变化。** 沿用 repair-state，保存包括通过状态在内的实际壁厚观测，返回前值/现值/差值及采样、风险位置、网格、坐标系和口径变化。跨模型、意图、零件、配置或测量口径不满足条件时明确不可比。阶段未重测时不会把旧 warning 消失视为解决，原有 error 生命周期不变。
3. **测成品，并以共同视角比较修形。** `a3d measure` 直接读取 STEP 的指定截面，报告外包络宽深/中心、材料岛、孔及明确平面；不读取 STATIONS，也不凭截面孔推断装入路径。完整数据落盘，stdout 最多展示 8 个截面并给报告 hash。`a3d compare` 用共同坐标、相机和缩放生成前后图及投影占用差异，输入和图像都有 hash；比例变化和位移不会被自动对齐消除，投影相似度不作质量分数。
4. **完整登记前的草模预览。** `a3d draft SOURCE.py` 与 `export_draft(parts, references=...)` 在独立 `.amagine3d-drafts/<runId>/` 生成明确标记的 STEP、GLB、PNG 和 draft 结果。它不要求 intent/feature，不写正式 scene、report 或发布指针。草模几何尚未通过安装、打印或最终验收；正式 `compile` 拒绝用 draft API 交付，仍要求原有完整合同和检查。运行复用托管 Python、workspace 路径边界及子进程总超时；作者 Python 本身不是操作系统沙箱。

冻结前本轮共 **153 项相关检查通过**，不与前轮重叠测试相加，也不宣称全仓测试：compiler 44、printability 37、成品测量 13、draft 8、前后图 6、已有 renderer 12、材质渲染 1、capability 12、CLI 12、共享文件 4、真实公共建模示例 4。最后四项包含曲面两次实际编译和完整安装模型。

独立实物复测发现并修正了多实体截面坐标兼容及截面积默认积分精度问题。最终杯子 STEP 的 z=95 外宽为 **52.3226800973 mm**，z=0 外宽 **50.0000002 mm**，上/下外包络中心差 Y 为 **4.0006879183 mm**，底面积为 **1945.0619284 mm²**。这些是既有工件的测量，不能称其缺陷已被工具改好。

两份历史杯子 STL 重新走 QA，最低值、P05 和采样违规数值逐项保持不变。将结果注入对应完整 compile-result 后，紧凑输出为 **7,128 / 7,157 字符**，定位和比较都保留；比较明确标注样本及风险点已变化，不推断修改的因果。实际草模公开命令约 2.875 秒生成 3 个实体；指定截面量得 47×35 外廓和 34×22 内孔。随后真实 `compile` 对 draft API 及空特征合同均拒绝，预置的正式文件字节不变。

证据：[QA 原工件复测与输出预算](../workspace/skill-ab-v4/inspection/qa-warning-feedback/verification.json)、[成品杯子截面](../workspace/quality-feedback-checks/seed-cup-sections.json)、[共同视角杯子图](../workspace/quality-feedback-checks/cup-top-comparison.png)、[draft→measure→compare 与正式编译隔离](../workspace/skill-validation/draft-installed-smoke/public-smoke.json)。

本轮增加了工具代码和维护成本，主 SKILL 从 199 行变为 215 行；不能宣称系统总量减少。新增内容主要是可选入口及输出解释，没有把每项工具变成所有任务的必经步骤。

### 真实模型对照安排

[v5 评估脚本及冻结](../workspace/skill-ab-v5/README.md) 固定两组共四次运行，每次 20 分钟、最多两次并发，沿用相同的既有模型配置。先运行杯子对，再审查并运行外壳对。

杯子改为**修复已有工件**：两臂得到同一份 v4 最后完成编译的源码、intent、打印配置、STEP 和 display GLB，均逐项核对同一 runId 的 hash。要求修正脚边薄楔并恢复明确 z=95 平面的 54 mm 成品顶口，同时保留原始造型要求。没有带入旧 scene、report、发布状态或旧 marker；runner 在导入这些文件前创建新会话 marker。该任务与此前从零生成杯子的用时不可直接比较。

外壳仍使用原始完整 brief，从空白会话开始。草模只计为早期未完成预览，不能计为制造件交付。新工具未被实际调用、末尾源码未编译或模型超时，都按实际状态记录；不事后代写模型来补齐实验。

### 第 5 轮实际中止与修复

真实会话暴露本地全权限测试未覆盖的问题：候选的 `measure` 使用 `Path.resolve(strict=True)`，访问受限的 session 父目录元数据时失败。两次已完成命令均 exit 2，第三次改用临时目录的尝试未返回便被中止。为修复工具后重测，两臂在约 **351.1 秒主动中止**；这不是 20 分钟超时，也不能用于评判最终建模质量。外壳没有启动。

候选/基线首个当前编译预览分别约为 **168.0 / 197.2 秒**，但都只是 seed 原样重建。STEP hash 的变化只来自导出时间戳，源码和几何没有变，不能称为修形成功。基线结束前改过源码但没有再编译；候选没有改源。候选确实尝试使用新测量命令，尚无 compare/draft 调用。首轮 witness 已进入输出，但没有已完成的据此修复。证据：[v5 最终流程](../workspace/skill-ab-v5/inspection/cup-process-final.md)、[主动中止原因](../workspace/skill-ab-v5/inspection/pilot-interruption.json)。

独立审查还发现 `compare` 接受 3MF，但当前托管环境缺少 Trimesh 读取它所需的可选依赖。现明确限定为 STL/GLB，并返回使用语义 display GLB 的提示；7 项对比测试及同一合法 3MF 工件的友好拒绝回归通过，没有安装依赖或扩大导入器。该修正不修改 v5 冻结版本。证据：[合法 3MF 复现](../workspace/quality-feedback-checks/review/compare-3mf-reproduction.json)、[修复后结果](../workspace/quality-feedback-checks/review/compare-3mf-retest.json)。

## 第 6 轮：修复真实沙箱兼容后复测

`73feb7f` 将测量入口的两处 strict resolve 改为现有工具采用的非 strict 解析，保留文件存在、路径边界、symlink/hardlink 输出保护。新的 macOS 回归先用 `/usr/bin/sandbox-exec` 拒绝父目录 metadata，确认旧解析方式确实 EPERM，再验证相对、绝对和同目录 symlink 三种 STEP 输入测到正确的 20 mm，并核对输入及报告 SHA。测量测试共 14 项通过；本轮各相关套件合计 **155 项**（替换前述测量/对比套件计数，不重复累加）。

同一真实 sandbox 条件下，compare 和 draft 的三种输入形式共 6 次公开 CLI 验证也全部成功。草模结果始终未通过最终验收，预置正式文件字节保持不变。证据：[真实 sandbox CLI 联调](../workspace/quality-feedback-checks/review/sandbox-cli/verification.json)。这项限制只用于重现祖先目录访问问题，不等同于复刻生产沙箱的全部规则。

[v6 冻结](../workspace/skill-ab-v6/README.md) 的候选为 `73feb7f`，基线仍为 `46c8d77`，其余任务、输入、runtime、模型设置和 20 分钟截止保持与 v5 相同。重新运行杯子修复对照及外壳对照；v5 主动中止记录不混入本轮完成率或用时统计。

### 杯子修复对照

两臂都在 **1200.015 秒超时**，均无最终回复。源码与最后完成编译的工件匹配，没有末尾未编译修改。基线只重建了 seed；候选在一次构造断言失败后生成了真正改变几何的版本。

| 观察 | 基线 | 候选 |
|---|---:|---:|
| 首次当前预览（均是 seed 重建） | 286.3 秒 | 313.8 秒 |
| 首次实际修形预览 | 无 | 869.4 秒 |
| 真实 compile 调用 | 1 | 3，含 1 次失败 |
| measure 成功调用 | 0 | 4 |
| compare 成功调用 | 0 | 1 |
| draft 调用 | 0 | 1，失败，无草模产出 |

冻结统计脚本将基线一次 `compile --help` 误计为编译。这里使用独立事件审查纠正后的次数，保留原脚本及 [纠正记录](../workspace/skill-ab-v6/inspection/cup-compile-count-correction.json)，没有在对照中修改评估变量。

候选实际用 `measure` 读到 seed 顶口 **52.3227 mm**，修形后再读到 **54.1658 mm**，并在公开消息中引用结果。新命令的真实 sandbox 问题已不再出现。`compare` 成功输出共同前视图，模型引用了 0.994 的投影重叠率；该值不证明局部厚度、遮挡处或整体质量。SDK 事件未单独暴露看图工具，不能只凭模型说“已看图”增加独立看图证据。

`witness` 已进入编译摘要，候选后来用 `diagnose --full` 读取了口缘附近的最低样本及采样覆盖。没有观察到它明确采用 `warningComparisons` 的数值；冻结版还因纯打印平移变化把这次比较标为不可比，后续修正另述，不能算成模型当时已得到有效差值。

草模入口在这次已有模型修复中反而带来返工：候选直接把使用 BuildSession 的正式源码传给 `draft`，没有改成 `export_draft` 协议。命令失败后，它又读取日志和示例才返回正式编译流程，没有成功草模。`948fd10` 因此把已有的条件说明收窄为**新模型且主体/布局尚未确定**时使用；只改两处既有措辞，4 项共享文件检查通过。该说明修正晚于 v6 冻结，其模型行为效果尚未测试。

最终 STEP 的独立检查显示改善和退步并存：

- 顶口 z=95 外宽由 **52.3226800** 变为 **54.1657542 mm**，更接近要求但偏大 0.1657542 mm；没有另行发明公差判为达标。脚底 z=0 外宽仍为 **50 mm**，候选总包络约 **82.110×62.304×95.000 mm**。
- 同样 96 条脚缘定向竖线，最短材料段由 **0.21855** 变为 **7.24558 mm**，小于 2 mm 的样本由 **47** 变为 **0**。这批有限样本支持原脚缘薄楔改善，不能证明全局最薄壁。
- 另做每臂 840 条 BRep 法向材料弦检查。基线最短 **0.68133 mm** 位于脚部；候选 839 条有效、1 条跳过，其中 **26 条低于 2 mm**，最短 **1.55602 mm** 位于口缘。已在最短点的进入和退出前后做独立实体分类，确认是真实短材料跨度；不能用 X 向外/内宽差的一半约 5.58 mm 将它否定。
- 中央及附近 5 条竖线量得腔底由 **3.5** 变为 **4.0 mm**，这些位置上方到杯口没有材料阻挡。它们是有限通路检查，截面一个孔也不是全腔贯通证明。

当时据此判定口缘不满足 2 mm；**本轮方法审计撤回这一单凭短材料弦作出的壁厚判定**，见下文第 7 轮的方法更正。口宽误差及未完成交付的结论不变，仍不能把“脚缘修好”推广成“模型质量已提升并合格”。证据：[最终执行流程](../workspace/skill-ab-v6/inspection/cup-process-final.md)、[调用、反馈采用及工件绑定](../workspace/skill-ab-v6/inspection/cup-process-conclusions.json)、[独立成品复核](../workspace/skill-ab-v6/inspection/cup-independent-review.md)、[口缘最短点实体核验](../workspace/skill-ab-v6/inspection/measurements/desk-cup-d144c98d6e4c/rim-minimum-verification.json)。

### 后冻结修正：允许一致口径下的纯打印平移

v6 修形前后的打印变换只从平移 **[41.06244, 31.05869, 0]** 变为 **[41.05499, 31.05429, 0] mm**，这是按新包围盒居中产生的变化。旋转、单位尺度、算法、目标、意图和打印配置均未变；将整个 context 做等值比较过于严格。

`9569c5d` 仅在两端绑定有效、单位为 mm、刚体单位尺度且旋转一致时允许平移变化。风险点与包围盒八角先逆变换到统一语义坐标再比较，新增 `translationNormalized`，保留 `coordinateFrameChanged`。旋转、尺度、单位、测量口径或无效绑定仍拒绝；缺定位时返回位置变化未知。1e-5 mm 是输出位置的比较精度，不是物理 QA 门限。

47 项 compiler 与 37 项 printability 检查通过，其中新增 3 个独立测试方法；本轮相关检查总数更新为 **158 项**，其余套件沿用前述已完成回归，不重复累加重跑。两份 v6 STL 重新测量，minimum、P05、风险面积比例逐项与原 QA 一致。修正后重放返回 `measured`：minimum **0.00446→0.00725 mm**、P05 **3.5→2.68489 mm**、风险面积比例 **0.040005→0.008871**，并保留采样变化、真实风险位置变化和未消失的 warning。各指标方向不同，不能合称质量改善。

完整紧凑输出为 **7,197 / 7,278 字符**，原 12,000 字符预算和 QA 门限未变。证据：[实际工件重测、哈希与后冻结范围](../workspace/quality-feedback-checks/review/warning-translation-v6/verification.json)。这是工具修复后的独立验证，**没有进入 v6 模型运行，也不能算模型实际采用了修正后的反馈**。

### 后冻结修正：让草模入口错误直接可处理

杯子与外壳候选都误将 BuildSession 正式源送入 draft，说明只提供独立接口和示例不足以避免误用。`bff71ce` 在现有总帮助、draft 帮助及 `export_draft` 能力首段直接说明：草模源调用 `export_draft(parts)`，已有正式 BuildSession 源走 `a3d compile`。源码失败保留日志、最多 1,600 字符的具体错误尾部及条件式修复提示；未输出 draft manifest 时也给出入口说明，超时行为不变。没有加入自动转换或绕过正式合同的适配器。

扩展原有 8 项 draft 和 12 项 CLI 用例后全部通过，独立测试方法数未增加，总数仍为 **158 项**。同一 v6 误用源码 SHA 的隔离复测保留了被 AuthoringError 包装的 IsADirectoryError，并给出正确入口提示；它在读取配套参数前已失败，未复建完整原会话输入环境，临时复测文件也未保留为独立证据包。正常 `export_draft` 源仍生成 STEP、GLB 和预览。实际能力查询也返回了新入口说明。

这项修正晚于 v6 冻结，测试证明提示及错误保留行为，**尚未验证它能让模型避免误用或完成更好的几何**。

### 外壳从零建模对照

两臂均在 **1200.052 秒超时**，没有最终回复。终态库存中均没有 STEP、STL、BRep、3MF、GLB、scene、成功 build report 或预览，因此没有可比较的外壳成品。

| 实际行为 | 基线 | 候选 |
|---|---:|---:|
| 首次几何 build 源写入 | 815.171 秒 | 681.895–682.013 秒 |
| 首次有效意图返回 | 441.272 秒 | 718.896 秒 |
| compile 调用 | 4 | 9 |
| 配置阻断 / 几何失败 / 成功 | 0 / 4 / 0 | 6 / 3 / 0 |
| draft 调用 / 成功 | 0 / 0 | 1 / 0 |
| 草模或正式预览 | 无 | 无 |

候选通过 shell heredoc 一次写入 intent/build，表中范围是该命令的起止时间；SDK `file_change` 为空不代表没有写源。两臂均无 compile 帮助误计；独立审查也排除了把源码注释中的 “installation witnesses” 误判成 QA 定位查询的启发式误报。这里采用纠正后的实际事件分类。

两臂首次意图生成都因后表面方向字段错误失败，基线另有未声明 critical feature 引用。候选虽更早写出几何源，草模却在完整意图通过后才调用，且仍误用正式 BuildSession 源；只有失败 result/log，没有早期草模及转正式模型的完整过程。后冻结提示修复不能计入本次结果。

基线 4 次编译均为 `SOURCE.UNION_DISCONNECTED`：前两次电路板支撑得到 4 个分离实体，后两次后盖得到 3 个。候选前 6 次被 marker 与输入文件的时序阻断；反复刷新 marker、重跑保持原 JSON 的意图生成器、原样重写 build 都没有解决。第 7 次通过 `touch` 更新原 intent/build 的时间戳后开始几何执行，源码 hash 未变；这是时序处理，不是新的几何修改。之后依次遇到屏幕支撑 3 个分离实体，以及两次 `housing-collar-socket` 切除体积为 0。

最后两份源码均精确匹配各自最后失败 attempt 的 source-preflight，没有末尾另改源码的字节差异。原通用快照目录 `uncompiled/` 只代表缺少成功 report 的绑定，不能按目录名推断最后又改了未尝试的代码。共 44 个终态文件副本已逐字节核验；残留意图、能力清单和失败诊断不作制造工件。

原始要求的两制造件、100×40×70 mm 包络、显示窗口、电子舱、USB 贯通、可拆后盖、模块支撑和装入路径、壁厚均**未测且未通过验收**。这些要求仍然适用，缺少工件不是“不适用”。没有补造几何来替代失败的模型运行。证据：[外壳最终流程](../workspace/skill-ab-v6/inspection/enclosure-process-final.md)、[命令、时序与 hash 分类](../workspace/skill-ab-v6/inspection/enclosure-process-conclusions.json)、[终态库存及独立核对](../workspace/skill-ab-v6/inspection/enclosure-independent-review.md)。

### 本轮结果与保留范围

四项功能都已实现，相关 **158 项回归通过**，并用真实 BRep、网格、图片、公开 CLI 和受限父目录场景验证。它们支持的确定结论是：最终尺寸可以直接测，已有薄点不会被摘要丢失，同口径告警可以追踪，修形图保留共同尺度，草模与正式交付分离。

真实模型对照共 4 次，均在 20 分钟内未完成交付。杯子候选实际用了新测量与对比，脚缘改善，但口宽仍偏差，口缘短弦须按第 7 轮更正后的方法重判；外壳无成品，草模在两种案例中均被误用。**这些结果不支持声称整体建模质量或完成率已提高。** 每个案例每臂仅一次，前后冻结之外的修复仅做工具回归，不能给出统计显著性或完整因果归因。

主 SKILL 由 199 行变为 215 行，工具代码及维护成本增加。新增功能保留为按需调用的实验能力；关键修复放在确定性工具及就近帮助中。当前证据仍显示，合同/时序返工、实体连接、装配构造与对测量含义的判断会决定最终结果，增加规则条目不能替代这些判断。

全部实现与说明保留在 `codex/skill-simplification` 的独立 worktree，相关工具修复、接口说明和评估报告分别提交，原工作区未合并。可根据这些证据分别取用和回退，无须一次接受全部实验接口。


## 第 7 轮：先修复已复现的建模阻碍，再做冻结验证（进行中）

本轮基线固定为 `b8486d110ce9d8c6cd681b6982a3d641bb4a37a9`。开发只使用已经暴露的杯子和外壳；新的四任务、每任务两次重复的配对验收在候选冻结前封存，主指标、失败处理与停止规则见 [预注册](../workspace/quality-evaluation-v1/preregistration.md)。尚未取得新一轮模型质量提升证据。

### 测量解释更正

v6 口缘 **1.55602 mm** 是有效的材料弦，退出点位于 z=95 的顶部端面；它没有证明射线两端是一对相对内外壁。独立已知 **3 mm** 的平行斜壁加 0.5 mm 端口圆角，也会在靠近开口处产生 **1.83848 / 1.41421 mm** 的端面截断弦，而中部实测为 3 mm。因此不能把所有短弦直接判成侧壁不达 2 mm。

真实 1.5 mm 薄斜壁和外伸薄楔的阳性对照仍被测出。这里修正的是物理量解释，**2 mm 壁厚与 3 mm 底板要求未变**；所有 v6 原始 STEP、点位和测量数值保留。该更正也不自动证明 v6 候选口缘或全局壁厚合格。应区分相对壁、底板、端口终止与外薄片，结合实际面片和材料见证段判断；无法分类的项记未定。证据：[方法审计及解析 BRep 对照](../workspace/quality-evaluation-v1/method-audit/measurement-review.md)。

### 本轮候选干预

- **同一份几何源从预览进入正式编译。** `BuildSession(part_names=...)` 可在完整 intent 之前建实体并 `export()` 预览；已有合同的源码使用 `draft --intent`。正式编译仍核对完整部件/特征/所有者及全部验收。显式 intent 必须与托管草模选择一致，预览输出强制隔离，草模不能替代正式交付。
- **去掉手工输入时间戳门槛。** 编译按本次 runId 记录 source/intent/profile 的稳定 SHA、大小和时间，并在执行、QA 后复核；输出仍使用本次自动 marker 和完整绑定核验。旧 `--marker` 可作为出处保留，其年龄不决定输入有效性。任意 Python 导入依赖尚不在输入快照覆盖范围。
- **布尔失败返回实际空间证据。** 对 union 的每个 operand 组件区分连接/交集/表面距离/间隙，返回最近点和 bounds；compact 优先显示断开的组件。相同失败源码重放仍保留原错误码，分别测得 21.9、0.05、0.7 mm 间隙。不会用包围盒或零移除量冒称某个历史切刀的归因。
- **入口调整为先主体，再合同，再按需专题。** 主入口从 215 行 / 12,307 字节变为 169 行 / 9,423 字节；保留实用功能、打印体积、装入/支撑/保留、独立实物测量和意图版本要求。这个尺寸变化只是上下文成本数据，不作为质量提高证据。

确定性证据：[布尔失败重放](../workspace/quality-v7-audit/connection-audit/verification.json)、[连续两次无手工 marker 编译](../workspace/flow-audit/hash-provenance-01/summary.json)。公共示例回归验证单件草模→同源正式编译，以及已有 intent 的两件模块装配预览→正式安装、螺钉和打印验收；正式源码字节保持一致。

草模与正式源统一后，又发现原 `compile` 专用诊断在 `draft` 中丢失实测间隙。候选补齐同一诊断路径：草模立即记录本次 runId 的失败并抛出，复用已有摘要保留首个实测原因及完整证据路径；正式编译的延迟收集逻辑保持。真实草模 union/cut 测得 0.05 / 2 mm 间隙，捕获操作异常继续导出、或替换成旧诊断载荷也不能获得可用草模状态。没有新增失败预览框架。

本轮实现相关 **131 项独立回归通过**：53 compiler、5 freshness、8 CAD diagnostics、4 source diagnostics、17 BuildSession、12 draft、12 capability、4 shared-files、4 公共真实编译示例、12 CLI。重跑不重复累计，也不与上一轮 158 项简单相加。评测 harness 的无模型自检另记，不能替代模型结果。

### 已暴露任务上的开发检查

`dev-03` 冻结候选 `656a61fd3ead167bc6c3ab089b70894c6fe699ef`。杯子运行满 1,200 秒，三次草模成功，首张草模预览约 247.4 秒；使用了五次测量、一次共同视角比较。第一轮正式编译在约 669.1 秒完成，但现存测量记录显示包络 **82.077456×62.258062×95.0000001 mm**、z=95 顶口外宽 **54.697866 mm**，仍有实际尺寸偏差。模型后来改源，在第 1163.5 秒开始第二轮编译；截止时新几何与 build report 已产生，完整 compile-result 和预览仍属于旧轮。不能借用旧轮 `pass=true` 判新几何合格。

第一轮 STEP 已被第二轮覆盖，没有完整历史几何快照可供重测。第一轮数值只引用与旧 STEP SHA 绑定的既有测量及 QA；第二轮保存与截止库存匹配的工件用于开发诊断，但标记未完成交付。证据：[杯子执行流程](../workspace/quality-evaluation-v1/development/dev-03/inspection/cup-process.md)、[独立交付复核](../workspace/quality-evaluation-v1/development/dev-03/inspection/cup-independent-review/review.md)。

同批外壳在模型调用前遭遇评测器共享配置文件的写入竞争，约 0.253 秒即退出；没有模型会话，不记作 CAD 产品失败。评测器改为完整临时文件加原子独占发布，9 项无模型测试通过。`dev-04` 保持同一候选提交，只运行外壳开发检查，原失败记录保留。这些开发运行不混入尚未启动的正式配对统计。

第二轮新 STEP 的独立复测为 **81.882125×61.943502×95 mm**，z=95 顶口 **54.772784 mm**，说明最后改动又影响了此前测过的尺寸。旧评估曾据 96 条脚缘探针及 840 条法向弦判定脚缘局部改善；后续 `dev-05` 交叉检查发现材料射线算法会漏掉退出后重入，先暂停了探针解释，再对 seed、dev-03、dev-05 同法重算，结果见下文。另有独立面片距离测量：128 对实际主内外壁面最短距离为 **4.730535 mm**，最短点在两片修剪面内部、法向相对；整块内底的向下实体棱柱全部包含于成品，平底厚度 **3.5 mm**。不同方法的证据分开保留，有限射线不称作全局最薄壁证明。尺寸失败及第二轮未完成交付的结论不变。

`dev-04` 外壳运行满 **1200.1 秒**，6 次草模中 1 次成功，首张预览 **205.926 秒**，正式 compile 0 次。23 条完成 shell 命令累计实际执行约 **20.566 秒**；14 条文档/API 返回共 **75,825 字符**。工具间空档不能在缺少完整 SDK 事件的情况下硬归为阅读、思考或写源时间。模型越过了 0.2 mm 支撑断连，后来卡在定位槽切刀与壳体相差 1.8 mm 的实际几何问题；最终源匹配最后失败草模，19 个截止文件稳定，无迟到交付。证据：[外壳执行与因果审查](../workspace/quality-evaluation-v1/development/dev-04/inspection/enclosure-transport-review.md)。

### 针对开发失败的下一候选

1. **精确尺寸及近似误差分开。** `bdd01c6` 将公开 compile 的 BRep 设计尺寸验收改为实际 STEP 回读的 `1e-4 mm` 数值精度；四位小数报告另用 `2e-4 mm` 覆盖端点舍入。网格/文件近似匹配公差保持，合法显式范围继续有效。错误同时给目标、实际值和有符号差值。同源杯子独立草模重建得到完全相同的三个尺寸，正确拒绝 X/Y；倾斜曲面解析控制的 STEP 回读误差小于 `2.36e-12 mm`。独立 `step_check --intent` 入口的旧默认 0.5 mm 尚未改，本次保证的是公开 compile 必经链路。证据：[尺寸机制审查与控制](../workspace/quality-v7-audit/dimension-audit/findings.json)。
2. **成形后一起检查包络和关键截面。** 曲面示例把 brief 目标与可编辑 STATIONS 分开，在全部材料操作和圆角之后检查整个 BRep 包络及指定高度的成品截面，一次列出全部偏差。真实负例同时加大肩部和添加顶口圆角；草模在导出前同时指出 X/Y 包络及顶口偏差。正例及改变壁内缩量后的同 intent 编译均通过，没有新增检查框架。
3. **用现有示例减少坐标和 owner 返工。** 外壳初版将屏幕/PCB 的 Y 起点搭配 CENTER 对齐，实际分别错位 4 / 6 mm。安装示例现用本地 `interval_box` 复用模块、窗口、内腔和装入路径的明确上下界；不新增公共 API。所有多件 add/cut/observe 显式写 owner，入口也展示一次带 owner 的 cut，并让安装外壳更早指向安装示例。真实两件导出、模块支撑/装入/保留、螺钉及打印检查继续通过。这些改动不自动解决切刀位于空腔等构造错误。

截至这次候选冻结前，相关验证覆盖 **235 个去重测试方法**：前述 131 项、25 build manifest、14 intent revisions、1 BRep 解析控制、37 printability、27 color guardrails。其中这次精度改动真正新增 4 个方法；公共示例的正负例扩展及重复运行不增加方法计数。53 compiler 与 4 公共示例复测通过，最后修改的安装与曲面示例分别再次通过。验证日志保存在 [本次回归](../workspace/quality-v7-audit/final-regression/)。仍须用新一轮模型运行检查能否帮助收敛，不能以回归通过替代建模质量结果。

### dev-05：实际收敛与截止结果仍失败

候选固定为 `a2ff3454420c8792804e9cdc4914276846e46eb6`，杯子和外壳各运行 1,200 秒，均超时且没有合格最终交付。杯子曾完成五轮完整编译；逐轮保存的不可变快照独立测得 z=95 口宽依次为 **54.417775、54.872459、54.872459、53.000372、53.000372 mm**，均偏离固定 54 mm 目标。五轮包络约为 **81.999901253×61.999935192×95.0000002 mm**，说明严格包络检查被满足，却没有解决最终口宽。不能从中挑一轮编译 `pass=true` 作为任务通过。证据：[五轮历史尺寸](../workspace/quality-evaluation-v1/development/dev-05/inspection/cup-independent-review/all-successful-history-dimensions.json)。

截止时杯子又修改了源码，最后一次已完成编译仍失败，最新尝试未结束。原截止库存之后 5.4–12.6 秒又产生六个迟到文件，其中 STEP 被改写；它们单独保留，不归入截止交付。清理审查在三个时点通过命令行和实际 cwd 未发现残留的对应进程，未向未知进程发信号。评测器已补充精确会话归属的退出清理，原截止时间及库存保持不变。

外壳已采用 MIN 对齐区间盒和所有者参数，未再出现旧的坐标起点/中心或漏 owner 错误；但唯一草模直到约 **1076.1 秒**才启动，因合同未声明 `screen-lower-left` 失败，正式编译为零。现有安装示例的构造参数 JSON 由 intent 生成，实际迫使作者先准备合同，抵消了入口所说的先草模。14 次可见文档/API 读取共返回 **62,359 字符**；安装示例首次读取从 dev-04 的 188.316 秒提前到 **90.733 秒**，大篇 multipart 指导首次在 **899.464 秒**，发生于意图失败之后，不能把整个超时归因于先读大文档。证据：[dev-05 过程和因果审查](../workspace/quality-evaluation-v1/development/dev-05/inspection/development-transport-review.md)。

最后完整杯子快照的交叉检查还发现脚部真实回缩：z0 外宽 50 mm，z1.5 外宽约 **49.691147 mm**；`(24.9, 0, 0.5)` 和 `(24.9, 0, 3)` 在材料内，而 `(24.9, 0, 1)` 在外。旧 `material_line` 把这条固定射线含空气的路径报告成连续 **15.074963 mm**；旧 96 条标准脚缘射线的最小值则是 **14 mm**。这是两项不同记录，均不能采信为已证实的连续材料厚度。原报告保留，脚缘已修复的解释撤回，正负控制和所有相关历史样本按修正算法重算；壁厚 2 mm、底厚 3 mm 与尺寸容差均不变。证据：[脚部直接交叉检查](../workspace/quality-evaluation-v1/development/dev-05/inspection/cup-independent-review/foot-retreat-crosscheck.json)。

修复改用实体与有限线段的 BRep Common 提取材料子段，与原始交点和实体分类核对；冲突仍记录为未定，不跨空气合并。最终 **14 项评估器测试通过**，48 条已有控制射线（含旋转、细分）与旧值误差小于 `1e-6 mm`。同一组 96 脚缘射线重算后：seed 有 **40 条已确认 `<2 mm`、7 条未定**，最小 **0.218551 mm**；dev-03 的 **96 条全部一致，0 条 `<2 mm`、最小 12.389541 mm** 保持；dev-05 最后完整快照则恢复 18 个漏根，得到 **18 条 `<2 mm`、最小 0.475277 mm**，另 3 条全线存在局部冲突但已确认短首段仍保留。dev-03 的局部脚缘改善有修正后的证据支持，dev-05 没有；有限脚缘射线不证明完整模型或全局壁厚合格。证据：[同口径重算及输入绑定](../workspace/quality-evaluation-v1/development/dev-05/inspection/cup-independent-review/method-correction/reruns-final/summary.json)。

再按旧的 840 个物理采样点复核连续性，未重新挑选 UV 点：dev-03 为 **804 条一致、36 条未定**，已核对数值无变化、最小 3.5 mm；dev-05 为 **840 条一致、最小 5 mm**，均未采到 `<2 mm`。后者与实际脚缘短段同时成立，说明普通法向采样漏掉了脚缘小区域，不能据此宣称脚缘合格。dev-03 的 4.730535 mm 最短面距由独立 BRepExtrema 求得，三个最短解的材料见证均经新法确认；完整底板的实体棱柱包含检查也独立于旧射线方法。原始数值、失效解释与新法证据分别保留。

### 进一步减少作者返工

安装示例现在由 build source 直接持有构造参数，CAD 执行放在 `main()`；不需要 intent、打印配置或参数 JSON 即可完整草模。匹配的 intent generator 可导入参数而不构造几何、不写文件，固定外包络 **80×60×16 mm** 和采购模块 **50×30×5 mm** 要求独立于可编辑构造参数。真实测试先无合同草模，再有合同草模，确认两者几何一致并用同一份源码通过完整安装、螺钉和打印检查。没有增加第三个模板或公共 API。

该改动后四个公开编译示例与四个共享文件测试通过；新增一个独立目标测试把构造宽度改为 79/49 mm，确认验收仍为 80/50 mm 且无导入副作用。新增测试初稿误读 intent 的嵌套结构而失败，改为读取真实展开后的 `features` 后通过，两个日志均保留。相关历史覆盖增为 **236 个去重方法**，不表示在新版本完整重跑了全部 236 项。证据：[示例回归](../workspace/quality-v7-audit/final-regression/draft-first-examples.log)、[独立目标回归](../workspace/quality-v7-audit/final-regression/independent-installation-targets-fixed.log)。

草模输出再复用现有 BuildSession 已登记的 `id/owner/role`，通过 `constructionFeatures` 交给合同作者，避免凭记忆重新抄写特征名。它没有验收字段，明确不是需求、不是完整操作清单；无 intent 的部分 finish 和直接原生编辑可能不在其中。参考件不混入制造特征，非法 owner/role 或额外验收字段被拒绝。仅扩展现有 `export_draft` 可选参数和 JSON，不自动生成合同，也不改变最终编译分支。

这项改动的 **32 个独立测试方法取得通过结果**：17 BuildSession、13 draft、2 capability；只新增 1 个非法元数据方法，其余扩展旧测试，相关历史覆盖变为 **237 个方法**。首轮为 31 通过加 1 个旧 CLI 测试的 ASCII 解码异常；设置 `PYTHONUTF8=1` 后原样单项重跑通过，未改变断言或产品行为，两次日志保留。动态特征名、所有者、observe 角色、finish 缺项、低层省略兼容和草模不能进入最终验收均已覆盖。证据：[首次验证](../workspace/quality-v7-audit/draft-construction-check/tests.log)、[UTF-8 单项重跑](../workspace/quality-v7-audit/draft-construction-check/utf8-retry.log)。

另做了一个开发用数值原型：只调杯子三个构造控制，用最终 BRep 的包络宽、深、口宽组成残差，有限差分估计耦合后联合修正。**9 次几何评估、25.9 秒**收敛；最终 STEP 读回为 **81.999999992、61.999999973、54.000000605 mm**，原 intent 字节不变。这个手工编排的局部求解仅证明尺寸可收敛，脚部回缩仍未修复，不能计作模型运行通过或整体建模质量提高。证据：[联合尺寸原型与全部轨迹](../workspace/quality-v7-audit/coupled-dimensions/README.md)。正式四任务配对验收仍未启动，保持封存。

把联合更新核心放入现有 `surface-shell.md` 的条件指导，未新增运行时 API 或主入口规则。公开代码原样提取后重用同一个实际成形 BRep fixture 验证，**11 次几何评估、30.95 秒**收敛，STEP 三个误差最大约 **`6.05e-7 mm`**。该验证比原型多两次当前点重复测量，不能当作新的模型成功次数。指南同时说明短实心脚部可单独控制最低一段，但实际接合、厚度、尺寸及造型仍需复核。证据：[公开片段绑定](../workspace/quality-v7-audit/coupled-dimensions-public-check/provenance.json)、[实际测量轨迹](../workspace/quality-v7-audit/coupled-dimensions-public-check/result.json)。

评测器另通过 **23 项无模型清理/集成测试**：仅处理按实际 cwd、argv 和脚本位置确认属于选定会话的 Python/Node 进程，每次发信号前复核 PID 身份；未知归属、共享服务或发现失败会阻断后续运行。两臂独立清理，保留原 1200 秒截止与 launcher 宽限，不把后续清理期算作建模预算。只有稳定扫描后才首次写 after-exit 库存，迟到文件不会替代截止结果；冻结的 dev-05 完整性再次通过。证据：[清理测试与最终文件绑定](../workspace/quality-evaluation-v1/inspection/cleanup-harness-verification.json)。

### dev-06：脚部构造被采用，完整交付仍未通过

`dev-06` 冻结 `587b236618b7f489deeb2bcfe00ee4d2bf71ab7e`，杯子与外壳各运行 1,200 秒。杯子在约 309 秒读过联合尺寸和短脚部指导，随后采用 **7 mm 实心脚部挤出与 loft 合并，再统一挖腔**；没有观察到联合尺寸求解。5 次已完成正式编译中，只有第 5 次在约 **1066.3 秒**成功。另一轮曾通过源内尺寸断言，但 STEP 回读 Y 比目标小 **0.000153 mm**，仍被原 `1e-4 mm` 验收拒绝，没有扩大精度。

唯一完整成功快照的 STEP 为 **81.999909979×61.999996958×95.000000200 mm**，包络通过；z=95 顶口外宽 **53.594642038 mm**，不满足 54 mm。修正后的独立方法复核同样 96 条脚缘线：全部交叉核对一致、0 条 `<2 mm`、最短 **12.363200 mm**；5 条中央底板线为 3.5 mm。这支持本轮有限脚缘样本改善，尚未完成全局壁厚或完整质量验收。首个和最后成功快照实际是同一个，只测量一次，没有重复计数。证据：[独立几何复核](../workspace/quality-evaluation-v1/development/dev-06/inspection/cup-independent-review/review.md)。

模型后来读到顶口偏差并尝试修正，在截止前约 2 秒再次改源并启动编译。截止源码与上述成功快照不同，当前 build report 缺失，旧 STEP 与预览未被新一轮完整交付替换，故最终交付仍失败。成功历史快照及截止源码分别保留，不能择取历史成功替代终态。清理按实际身份终止了仍属于杯子会话的 Node 子进程；其他候选进程复核时已退出，没有向它们发信号。截止、after-exit 与随后复核库存相同，本次未发现迟到文件或残留会话进程。

外壳截止时没有源或草模。最后完成工具调用在 **861.069 秒**；约 **970.882 秒**明确出现 SDK `stream disconnected before completion` 重连错误，之后没有新的模型工具结果。因存在实际传输故障，不能把全部零产出归因为 skill、文档阅读或建模决策，也不能记作质量通过。原失败记录保留，`dev-07` 用同一冻结版本补跑外壳，并与原记录分别报告。证据：[完整执行、错误和清理记录](../workspace/quality-evaluation-v1/development/dev-06/inspection/development-transport-review.md)。

### 把校准步骤接到可执行的完整几何

针对 dev-06 仍手动调整耦合尺寸的行为，现有曲面示例改为 `build_geometry(controls)` 和 `measure_finished(controls)`：每次创建新的完整外形、腔体及证据，所有修饰位于同一函数内；最终导出调用同一构造。导入不执行 CAD 或导出。指南直接调用这两个函数，仍只给有限差分联合更新和有界试探，不新增运行时 API、自动放宽公差或主入口规则。普通 Python 校准与托管 draft/compile 分开，失败的托管运行不能捕获错误后冒充成功。

隔离原型中，重构前后完整 compile 均通过，intent 字节不变，STEP 包络同为 **100.0000002×80.0000002×90.0000002 mm**、体积同为 **98981.68638693447 mm³**，顶截面为 82 mm。5 次新建几何检查确认参数作用、失败回滚及证据隔离；添加 0.5 mm 顶口圆角后，回调实测 **81.236571623 mm**，正式编译也在导出前拒绝同一顶口误差。原型首轮曾错误预期控制深度 79.6 必然对应成品 79.6；实际因另一个偏心截面约束下边界而得到 **79.8 mm**，修正预期并保留首次日志。这正是回调必须测量最终几何的原因。证据：[原型说明及完整结果](../workspace/quality-v7-audit/surface-shell-callback/README.md)。

已有曲面示例测试扩展后 **42.348 秒通过**，覆盖不改 intent 的壁内缩量重编译、普通进程回调的实际耦合尺寸与无文件修改，以及包络和圆角顶口同时偏差时的拒绝。相关历史方法覆盖仍为 **237**，没有将这次重跑加成新方法；没有全量重跑 237 项。证据：[最终示例回归](../workspace/quality-v7-audit/final-regression/surface-shell-callback.log)。这是示例正确性验证，模型是否采用并完成交付仍需下一轮开发运行。正式四任务配对验收继续封存，当前不宣称整体建模质量提高。

### dev-07 与后续入口重排

`dev-07` 外壳保持 `587b236`，在 **1200.032 秒**截止。13 条完成命令包括 12 条文档/API 查询（返回 **71,734 字符**）和 1 条打印配置命令；没有 build、intent、草模或制造工件。它在约 **826.543 秒**再次出现 SDK 断流，之后仍有两条成功工具调用，不能描述成完全停机，也不能假定恢复没有损失。与此同时，采用 `644164b` 的独立 `dev-08` 杯子运行在同一 UTC 时间窗出现相同错误，两个错误相隔 **0.654 秒**；其后杯子仍产生草模和 STEP 测量。两轮候选和开始时间不同，分开报告；这项共同异常不支持判断具体根因或损失量。原错误及后续事件逐条绑定，没有自动设施补跑或扩展截止。证据：[dev-07 终审](../workspace/quality-evaluation-v1/development/dev-07/inspection/development-transport-review.md)、[跨运行错误绑定](../workspace/quality-evaluation-v1/development/dev-07/inspection/provider-disconnection-crossbinding.json)。

dev-07 清理没有发现残留进程，没有发信号；随后库存仍仅有打印配置，无迟到或修改文件，冻结完整性通过。该运行未取得模型交付，且受实际传输异常影响；文档查询时间与无源码现象是观察，不直接等于 skill 的因果结论。

独立文档审查确认了一处可直接修复的顺序冲突：主入口要求无合同先草模，`authoring-example.md` 的第一个命令块却先创建 profile/intent 并正式编译；开发阶段又直接引向完整安装验收。现将已有的复制 build、draft、看预览步骤前移，profile/intent/compile 后移，并把安装证据限定到最终阶段。依赖 intent 的 assembly/surface-shell 路径仍明确先完成 profile/intent，再带 `--intent` 草模。功能模型的空间、通路、支撑、保持和装配要求，以及供应商、打印和最终验收约束全部保留。没有改运行时 API 或示例几何；主 SKILL 保持 **176 行**。

同时区分曲面校准的两个入口：修复现有源时迁入回调结构，保留其 intent、part/feature IDs 和真实站点映射；指南中的直接 import 代码用于有配套 intent 的独立示例。不能用示例的 `surface-shell` 身份替代实际杯子的 `desk-cup` 合同。4 项共享文件/路由检查在最终文本上 **2.149 秒通过**，前一次 2.677 秒日志另行保留，仍是 4 个既有方法。证据：[最终入口验证](../workspace/quality-v7-audit/final-regression/staged-entry-final.log)。这项重排尚无模型行为效果证据，安装示例自身仍包含完整定位、承托和四组螺钉，其适配成本不会因文字移动而消失。

### dev-08：交付完整，但两个修复目标仍失败

`dev-08` 杯子冻结 `644164b06bff1921336acee04dba5479e3e16cd3`，在 **739.445 秒自然结束**，没有用满 1,200 秒上限。6 次草模成功，3 次完成的正式编译中 2 失败、1 通过；最后源码与通过轮 `5c29ebc3-9c0a-49de-9933-e57027de4d0c` 一致，32 个完整快照文件逐一匹配真实终态。自然结束使用 terminal inventory，未伪造超时 cutoff；86 个终态文件稳定，没有残留或迟到文件。这次工件绑定完整，但这不是几何验收通过。证据：[终态绑定](../workspace/quality-evaluation-v1/development/dev-08/inspection/completed-run-terminal-binding.json)、[执行记录](../workspace/quality-evaluation-v1/development/dev-08/inspection/development-transport-review.md)。

独立 STEP 检查的包络为 **82.000004415×62.000008048×95.000000200 mm**，通过固定尺寸；z=95 顶口 **54.637931496 mm**，仍不满足固定 54 mm。96 条脚缘探针全部交叉核对一致，但 **14 条 `<2 mm`**，最小 **0.655464169 mm**；五条中央底板均为 3.5 mm。最短点从外底平面进入，在下部同一 B-spline 外面退出、再进入，之后才到内壁；实际是外脚回缩薄楔，不能解释成顶部端口截断。独立截面外宽 **50 mm（z0）→49.717312826 mm（z1.5）→50 mm（z3）**，反证源码对低段单调性的注释。没有执行模型源码来替代独立测量，也没有把有限样本称作全局壁厚证明。证据：[脚缘定位与材料段](../workspace/quality-evaluation-v1/development/dev-08/inspection/cup-independent-review/short-foot-classification.json)。

26 条完成命令中，模型读了 `surface-design.md`，没有继续读取其中链接的 `surface-shell.md`，也没读新的回调示例；不能把这次较早完成归功于新回调。最终官方测量与独立复核绑定同一个 STEP，回复报出 54.638 mm 同时宣称修复完成。源码已把目标解释成“at least 54”，而固定 intent 的 feature acceptance 仍明确 54 mm。观察支持入口未到达和固定目标被误读，不足以断言模型明知违规仍交付。

据此仅替换现有主入口的测量/范围说明：比较所有 intent 目标，包括 feature acceptance；固定尺寸在修复时仍是等式，显式范围另论；loft 或修饰尺寸漂移时直接进入现有曲面联合校准指导。主入口仍是 176 行。该文字修改及前述阶段重排尚需新的模型行为验证；当前不能宣称整体建模质量提高。

### dev-09：有完整历史编译，截止交付仍未完成

`dev-09` 冻结 `cf0ffa10512744bd4b79b82bb7a8eab0df0d9c3c`，同一模型、medium、每例 1,200 秒，杯子与外壳均超时，实际分别 **1200.047 / 1200.026 秒**。本轮未观察到 SDK 断流，仅有固定的启动实验功能警告；未自动补跑，也未启动正式配对验收。缺少错误事件不能证明基础设施完全无异常，工具间空档也不自动归因为阅读或思考。

杯子在 **267.698 秒**首次改源，**288.618 秒**完成修改后草模；6 次完成的正式编译中 2 次因整体 X/Y 失败、4 次 PASS，首次 PASS 为 **524.509 秒**。后续主要围绕局部薄壁告警调整内腔和口缘：内缩 6.0→6.5 mm 后告警几乎不变，圆角 0.5→0.25 mm 后采样更差，又回退0.5，最后改为0.75。截止时第7次编译未完成。最后完整成功轮 `94d4c3b3-fc1b-40bf-9c43-bab67ef1cb2e` 保有 **32 个文件、0 gap、同版预览**；其源码 `ed41162…` 与截止源码 `cc0cca8…` 不同，12个绑定文件已被后续执行改写或删除，不能混用历史 PASS 与当前 STEP/报告。独立几何质量结论另行复核，不能把编译 PASS 当成两个修复目标已满足。

外壳在 **890.317 秒**才写出第一份 build，先完成 intent/profile 并修正 face/normal 字段，**1069.313 秒**才完成第一次草模。三轮草模分别发现支撑导轨与壳体相隔 **0.5 mm**、螺钉盲孔未切到材料，最后在 **1190.604 秒**成功；正式编译为零。最后草模与截止源码 SHA `a186d56…` 一致。区间 Box 和显式 owner 已实际采用，不应继续把这一轮失败归为旧坐标对齐或漏 owner 问题。它没有完成正式安装与制造验收，也没有最终回复。

可见读取路线解释了下一步应检查的入口结构：杯子4次文档/API读取返回 **15,556 字符**，未读曲面联合校准指南；外壳首源前7次读取返回 **85,809 字符**，在读到完整 draft-first 主入口之后，仍先读通用连接、三份 build、两份 intent 和安装/打印资料。真正 copy→draft 命令位于 `authoring-example.md`，直到 **274.457 秒**才被读到，并与成套例子和后续合同步骤同批返回。这支持尝试把主入口压成当前阶段的一项可执行操作、将重复细节归回条件 guide；日志不能证明模型内部为何忽略顺序，也不保证文档压缩能提高最终质量。证据：[实际读取及阶段触发点](../workspace/quality-evaluation-v1/development/dev-09/inspection/stage-routing-review.md)。

清理按精确 session 路径、实际 cwd 和信号前身份复核，终止杯子仍运行的 CAD 启动进程；其他候选进程已退出，未声称杀掉它们。**03:18:43 UTC** 复核两例均无残留，杯子75文件、外壳16文件的 cutoff/after-exit/current SHA 映射完全一致，0晚写、0读取不稳定，原库存保留。证据：[终态与过程报告](../workspace/quality-evaluation-v1/development/dev-09/inspection/development-transport-review.md)、[截止绑定及清理复核](../workspace/quality-evaluation-v1/development/dev-09/inspection/post-cleanup-verification.json)。这轮仍未取得完整合格交付；下一轮保持原开发 case 与旧杯子 seed 合同原样，不暗补新增验收字段。

首个与最后一个完整杯子快照的 STEP SHA 不同，分别独立测量：两者包络均为 **82.000030656×62.000068419×95.000000100 mm**，均通过；z=95 顶口均为 **54.458773461 mm**，不满足固定 54 mm。两组96条足缘探针全部交叉核对一致、无短段或未知；最短连续材料段为 **12.387433451 / 12.910388563 mm**，五条中央底板探针均3.5 mm。源码、七个低位截面以及与底面相邻的8片侧面共同确认实际0–5 mm为独立直脚；最短射线穿过脚部后才进入内腔，不是此前的外脚薄楔，也不是主壁厚数值。这支持已覆盖脚缘的局部改善，全局壁厚及整块底板仍未证明。截止前模型明确说口宽“不小于54”，所以早期源码的 requested54 注释不能替代终态对固定尺寸的误读证据。证据：[独立几何和交付审查](../workspace/quality-evaluation-v1/development/dev-09/inspection/cup-independent-review/review.md)。

### 将截面要求交给最终 STEP 验收，并压缩当前阶段入口

新增的是可选的 `section_dimensions` intent 字段，复用既有 feature owner、固定/范围规则、STEP测量和公开 compile 必经的逐件 STEP 检查。它只描述指定绝对语义平面的整个物理所有者外包络，不能证明内孔、通路、壁厚或孤岛间连通。最终 STEP 使用既有 **0.0001 mm** 数值精度；独立 checker 的 `--tol 10` 也不能放宽它。空截面、错误所有者、print坐标、缺少BRep STEP和不匹配的合同/STEP SHA都失败。没有解析 acceptance 文本、增加新的 CLI 或自动修改合同。旧 intent 不带字段时仍没有这一截面验收；合法补入字段按已有版本规则属于 target-change。

配套曲面示例把已明确的82 mm顶口目标写入这个字段，删去19行左右的源内最终尺寸断言，保留完整几何回调作联合校准。源码 **115→97行**，intent **36→40行**。草模现在可以展示尺寸尚未合格的几何；正式编译在同版导出STEP上拒绝被圆角缩小的顶口。整体包络偏差仍可在源码导出阶段提前阻断，不能声称它与旧断言清单提供完全相同的同时反馈。复用旧合同的模型必须保留自己的尺寸断言，复制示例不会升级旧合同。

独立代码审查还发现两个需补的绑定边界：省略 `--intent` 或替换成无字段旧合同，会跳过独立 checker 的局部要求；现优先读取 report 的合同绑定，同一份字节做SHA及JSON解析，显式替代文件也必须匹配。另一个是多件导出保留的 `step:assembly` 汇总键与同名物理owner碰撞；该情形现在拒绝局部验收，单件名为assembly仍支持，没有扩大为导出格式重构。

主入口从 **176行/10,048字节→107行/5,894字节**。首次可执行入口变为选择一份build、复制并按brief改controls、draft、看预览；匹配intent的设置和安装/打印细节在几何可见后进入已有指南。原图像/供应商来源规则移入现有design-review，未删除。曲面例外在通用复制步骤之前说明，其必要intent设置后也先草模再compile。现有模型的耦合尺寸校准入口前移，不再依赖先读surface-design再点击深层链接。这个字数和路由变化尚无新模型效果证据，不能算作质量通过。

最终实现的 **130 个去重方法通过**：56项截面测量/manifest/intent revisions（55.559秒），以及74项公共真实编译示例/compiler/capability/共享入口（199.068秒）。真实STEP控制覆盖固定/范围、空截面、`--tol 10`无法放宽、X/Y/Z绝对平面与u/v方向；最终公开编译覆盖原示例、同合同壁厚参数修改、允许失败尺寸草模及拒绝最终顶口偏差。初轮公共负例曾错误预期包络失败发生于build-check，实际由源码export先拒绝；单独保存重现工件后修正测试预期，原失败日志不删除。独立checker绑定完善前的70项通过日志也按旧SHA单列，未冒充最终实现结果。真正新增仅3个方法，历史覆盖由237→240，不表示当前版本全量重跑240项。证据：[当前实现回归目录](../workspace/quality-v7-audit/local-section-contract-product-check/)。这些是机制与示例验证，下一轮模型运行仍须独立验收。

### dev-10：外壳草模提前，杯子足缘退化，最终质量仍未改善

`dev-10` 冻结 `f3610fdf4d1cf6408e49e30f940e514ed560b27e`，沿用 dev-09 的模型、medium、runtime、harness 和原开发 case，每例仍限1,200秒；杯子与外壳均超时，实际 **1200.042 / 1200.032秒**。杯子5次完成的正式编译中1次失败、4次PASS，首次PASS为 **636.302秒**，晚于dev-09的524.509秒；截止前约3秒又启动第6次，未完成。最后完整PASS `940b5ed3-6075-412b-a1a9-86fcf6b5884f` 的32文件快照完整，但截止源已将口缘0.5改成1.0 mm，当前 `desk-cup_report.json` 缺失，旧STEP不能反向绑定到新源，最终交付失败。截止及after-exit库存一致、清理后无残留或晚写；本轮无可见SDK断流，也没有自动补跑。证据：[执行和截止报告](../workspace/quality-evaluation-v1/development/dev-10/inspection/development-transport-review.md)。

首、末完整成功STEP分别独立复测，包络均为 **81.999981688×62.000080853×95.000000200 mm**，通过；z95顶口均为 **54.075825369496 mm**，仍不满足固定54 mm。相比dev-09的54.458773461 mm，偏差缩小但没有通过。两件同一组96条足缘探针均有 **32条 `<2 mm`**，最短 **0.303930057261 mm**，另2条整线存在未定区间；最短探针本身交叉核对一致。实际BRep面与共享拓扑边确认最短处是外底进入、同一外部B-spline面退出后再进入，属于真实外脚回缩薄楔；不是相对内外壁厚，也不是顶口端面截断。dev-09首、末快照的同组足缘样本均无短段或未知，因此dev-10在这组足缘指标上退化。五条中央底板仍各为3.5 mm，不证明整块底板或全局壁厚。证据：[独立几何及交付复核](../workspace/quality-evaluation-v1/development/dev-10/inspection/cup-independent-review/review.md)。

杯子首、末成功源仍把同宽z0/z3站点与后续站点一起smooth loft，没有独立脚柱或联合校准回调。原seed intent仍无typed `section_dimensions`，未被harness或模型暗补；因此不能把新增截面验收机制的公开回归当作这轮旧合同已检查54 mm。模型已读到同SHA官方测量的54.075825 mm，随后有“接近54”及“保持54”的可见表述；不能沿用上一轮“不小于54”的解释。其后主要调整口缘与内缩来响应局部薄区告警，未证明该告警属于相对薄壁，也没有证明最终壁厚合格。

外壳在 **80.508秒**复制现有安装示例，**399.344秒**取得首草模结果、**635.868秒**首次草模成功，均早于dev-09的1069.313 / 1190.604秒；本次确实先草模、后profile和intent。首草模前显式文档/API返回由dev-09的85,809字符降至 **40,855字符**，但后续仍陷入构造ID漏声明、非法kind、immutable intent修订和display引用绑定返工。6次完整compile全部失败；最后一轮已达到真实装配/安装审计，frame与cover重叠 **6544.021708 mm³**，屏幕与PCB插入路径分别和frame重叠约 **19372.000204 / 2345.999737 mm³**，后盖也阻挡free-travel。该失败轮65个绑定文件全部匹配截止，真实失败不能由草模提前掩盖。证据：[完整失败工件绑定](../workspace/quality-evaluation-v1/development/dev-10/inspection/complete-snapshot-cutoff-bindings.json)。

本轮只支持外壳更早获得几何反馈，**入口压缩未证明建模质量改善，杯子足缘反而退化**。杯子还经历了`a3d scene --help`被当文件、手写空scene、普通Python导入失败等绕路，之后才进入能自动刷新scene的正式compile；实际命令和可见消息已逐条绑定，不能直接断言删移一句说明导致了绕路。后续scene帮助、告警解释和构造ID衔接相关改动仍待模型效果验证，不能标为已成功或已解决；正式封存配对验收仍未启动。证据：[scene路线原始绑定](../workspace/quality-evaluation-v1/development/dev-10/inspection/cup-scene-detour-evidence.json)。

### dev-10 后：修复实际查询入口，保留物理失败，验证可行构造

模型确实读到了主SKILL前部的曲面联合校准说明，但没有执行；不能再把它归因为入口不可见。它实际调用的 `a3d guide strategy` 只有一般loft建议，现替换其中一条，直接指向已有 `surface-shell.md` 与完整 `build_geometry/measure_finished` 回调，不扩充主入口的曲面规则。主SKILL只恢复两行真实能力说明：`BuildSession.export()` 自动生成scene/report，compile的scene路径可尚不存在。`a3d scene -h/--help` 现在返回帮助；无参数及合法/非法JSON的原行为保持。六项真实公开CLI控制通过，普通JSON输出与冻结旧实现逐字相同。证据：[路由审查](../workspace/quality-v7-audit/surface-routing-review/review.md)、[公开scene控制](../workspace/quality-v7-audit/scene-help-proposal/product-cli-validation.json)。

单色/彩色QA与compact结果的厚度建议现在先定位实际STEP样本，区分真实薄壁/薄楔和曲率/自由口缘，再修改确认的问题。`max-sphere`不是自动识别相对壁面的测量；具体告警仍需定位，不能因此认定本轮告警为误报。只改建议，算法、样本、数值、严重度和通过条件保持不变，未知不变成通过。隔离AST与compact结果控制、独立产品审查分别保留。证据：[告警建议的依据与边界](../workspace/quality-v7-audit/thickness-advisory-proposal/README.md)、[产品审查](../workspace/quality-v7-audit/post-dev10-product-review/review.md)。

外壳隔离原型把七个内部支撑/导向步骤保留为 `finish` 内逐件 `checked_union`，把实际融合支撑材料观察到原有两项mount需求；原23-feature intent逐字不变。第一次观察Compound不水密失败，唯一修复仅融合观察材料。随后公开compile越过纯ID绑定障碍，最终STEP与原草模制造件的包络/体积差和双向BRep差集体积全为0；原来的6544.021708 mm³实体重叠、屏幕/PCB插入及free-travel失败仍然被拒绝。现有authoring指南用通用 `module-mount` 展示该模式，unknown ID错误指向它，校验仍严格拒绝未知ID。安装指南只替换原sweep段，要求它参与实际通道/支撑/停止行程的设计，引用已有内存检查。没有新API或新强制阶段，也没有把材料观察当作空间/安装证明。证据：[构造绑定等价实验](../workspace/quality-v7-audit/construction-feature-binding-prototype/README.md)。

杯子另做有界人工机制原型：从dev10最后完整PASS复制源码，原intent/profile逐字不变，三项站点控制在完整含圆缘几何上联合校准，并在原外loft、共用内腔之间融合真实3 mm短脚。9次校准约20.891秒，加1次公开draft和1次compile共11次构建；成品STEP包络 **82×62×95 mm** 在原精度内，z95口宽 **54.000000001272 mm**，同一96条足缘探针由32条短段降为0，全部明确，最短14.787807711 mm；五条中央底板仍3.5 mm。预览保留开口、收脚/丰腹/收肩/后移关系，短直脚台阶可见；这是非盲观察。局部mesh告警0.00722 mm仍未解决，有限探针不证明全壁厚。该原型证明这两项缺陷能在原约束下同时修复，**不代表模型自行采用、A/B通过或整体质量提高**。证据：[完整原型与64项SHA记录](../workspace/quality-v7-audit/cup-finished-geometry-prototype/report.md)。

当前最终代码合并回归 **150个Python方法通过（43.986秒）**，公开CLI **12项通过（1.963秒）**。未新增测试方法；首次混合suite因同名color `cad_helpers` 的搜索路径污染失败，既有布尔见证测试改为显式加载单色文件后重跑；另一次错写capability测试模块名的命令失败也保留，改用真实模块后通过。中间重复运行不累计。独立静态审查确认数值/严重度/验收逻辑未改；安装sweep中“is exact”的过强措辞恢复为“can be exact”。下一轮仍须冻结候选并保持原case/seed/模型/时限；当前没有正式holdout结论。

### dev-11：杯子同版完整交付，足缘局部改善，固定口宽仍失败

`dev-11` 冻结 `40fade6081c523a881dddc7bed705c70dca9c84f`，保持 dev-10 的原case/旧cup seed、runtime、harness、模型与medium设置，两例各1,200秒、并发2，无反馈或自动补跑。杯子在 **1132.362秒自然结束**，外壳在 **1199.986秒记为timeout**，保留原 **04:49:27.192 UTC** 截止；status时间比该截止早14ms的原始记录未改。杯子使用自然terminal inventory，不伪造cutoff。两例清理均未发现需结束的残留进程，也没有声称发过信号；随后杯子69文件、外壳78文件的terminal或cutoff、after-exit与current逐一一致，0晚写、0不稳定，冻结完整性通过。证据：[终态/错误链报告](../workspace/quality-evaluation-v1/development/dev-11/inspection/development-transport-review.md)、[清理复核](../workspace/quality-evaluation-v1/development/dev-11/inspection/post-cleanup-verification.json)。

杯子4次完成草模中3成功、1失败；6次完整compile中5失败、1PASS，首次PASS为 **895.653秒**，晚于dev-10的636.302秒。唯一成功轮 `73f5c571-ccee-4d3a-b72a-c77c40bb68ba` 的 **32个文件全部匹配自然终态**，当前源、STEP、报告与预览同版，没有将历史成功拼到后续源码。独立STEP包络 **81.999920795×62.000025204×95.000000200 mm** 通过原尺寸精度；z95外口 **54.099425219211 mm** 仍不满足固定54 mm，而且偏差较dev-10的54.075825369496 mm略增。最终回复报出了54.0994 mm却仍称修复完成，未明确承认这项目标失败。

同组96条足缘探针全部交叉核对明确、**0条 `<2 mm`**，最短连续材料段 **12.385417116704 mm**；五条中央底板均3.5 mm，实际底脚外宽50 mm。相比dev-10的32条短段/最小0.303930057261 mm，这组足缘指标有局部改善；当前源确实把6 mm直脚extrusion融合进外loft。最短材料段从外底进入，之后到内腔退出，不是相对内外壁厚。未采样脚缘、全局壁厚、整块底板、承载稳定性与盲评仍未证明；mesh薄区告警最小0.00706 mm保留，其实际原因未知，不能采用模型“仅为曲率采样”的未验证归因。证据：[独立同版STEP测量](../workspace/quality-evaluation-v1/development/dev-11/inspection/cup-independent-review/measurement-first/summary.json)、[独立结论与边界](../workspace/quality-evaluation-v1/development/dev-11/inspection/cup-independent-review/conclusions.json)。

这轮确实到达了曲面指导：杯子在 **299.313秒**读取完整`surface-shell.md`，**323.694秒**首次改源，随后采用真实直脚；不能再说指南未被读取。但它没有实现`build_geometry/measure_finished`联合校准回调，仍靠完整compile循环手调站点。五轮失败依次是X/Y漂移、Y=62.0484、62.0026、61.9996，以及STEP读回X=82.000116704超过1e-4容差；最后仅将整体包络调到允许精度，固定顶口仍漏过。旧intent未添加typed section字段，这次结果不能替代新合同验收机制的模型效果证据。

外壳在 **162.162秒**复制并读安装示例，**216.445秒**写适配源码，**227.103秒**完成首草模、**297.226秒**首次草模成功，早于dev-10的399.344 / 635.868秒。首次编辑前可见文档/API输出20,125字符，先草模后intent的顺序确实执行。后续 **11次完整compile全部失败**：构造ID未声明、intent文件缺失、修订元数据/归属错误、接口kind及接口ID不匹配，最后 **1186.527秒**失败为`interfaces[1].fasteners must declare at least one screw axis`。虽有intent中的四颗紧固件描述，实际source导出接口的轴向元数据仍不足；尚未进入完整物理装配/插入验收。最后失败快照13文件全部匹配cutoff，但缺1项且无同版完整预览，不能当合格交付。草模提前没有抵消正式交付失败。

可见SDK断流分别发生在杯子 **04:32:08.004 / 04:47:30.769 UTC** 和外壳 **04:48:10.127 UTC**，均显示`Reconnecting... 1/2`并在之后恢复可见工具进展；原预算不延长，不能把全部空档归因skill或思考。17条完整compile命令/事件输出均保留，5秒observer捕获其中16条/31次快照，漏掉的仅是外壳757.489秒“intent文件不存在”的CONFIG.INVALID结果，不伪造该轮快照；唯一PASS已完整保全。证据：[SDK原始事件绑定](../workspace/quality-evaluation-v1/development/dev-11/inspection/provider-errors.json)、[历史与终态逐文件绑定](../workspace/quality-evaluation-v1/development/dev-11/inspection/complete-snapshot-terminal-bindings.json)。本轮支持**交付一致性和已采样脚缘的局部改善**，仍不支持宣称整体建模质量提高；下一候选及正式holdout均未由这轮结论自动启动。

### dev-11 后：统一安装基准，补齐旧源迁移与成品尺寸守卫

安装示例改用前面−Y、后盖+Y、世界Z向上的共同基准，宽/高/深名称与人类尺寸一致。只替换原有build/intent两文件，保留原part/feature/interface及真实圆形紧固件recipes；默认世界XYZ由80×60×16变为80×16×60，独立目标仍为宽80×高60×深16。隔离对照的两件实体与原几何旋转后的双向BRep差集体积均为0，包络误差≤7.11e−15 mm；三项安装见证及四颗螺钉的轴/端点随同变换，两边安装审计均通过。支持方向−Y只检查向视窗座接触，不证明重力支持、承载或稳定；原两项薄区warnings保留。独立只读审查无坐标/归属阻断，实际接入后两项既有公开测试通过（25.480秒），覆盖导入不生成几何/不重写目标、真实两实体、插入/止挡/通路、四轴方向与端点。独立底插草模和历史记录不改。证据：[例子等价原型](../workspace/quality-v7-audit/front-service-example-proposal/README.md)、[独立审查](../workspace/quality-v7-audit/front-service-example-proposal/independent-review/review.md)、[产品真实测试](../workspace/quality-v7-audit/front-service-example-proposal/product-validation/receipt.json)。

旧cup seed从未带最终口宽断言，原“保留断言”措辞未覆盖这种情况。隔离副本只在现有export前加四尺寸成品检查，原intent/profile逐字不变：合格人工原型公开compile通过，实际顶口54.000000001277 mm；dev11终态在整体XYZ合格时以顶口54.099425219218 mm超差拒绝；同一负例源码的公开draft仍成功且deliveryReady=false。第一轮控制因函数内同名import遮蔽原调用而失败，完整保留，未算尺寸拒绝；后续draft本身成功，wrapper多段JSON解析错误单列，未计为几何失败。证据：[隔离控制目录](../workspace/quality-v7-audit/legacy-final-assertion-proposal/public-controls-v2/)。

产品仅在已有surface-shell指南把“保留”改为“保留或补上缺失检查”，以五行top-only `ValueError` 片段说明真实owner、固定目标、完整收尾之后/导出之前与draft豁免；不把重复目标断言加回已有typed验收的示例，不新增API或解析acceptance文字。该实际文档片段另有8项逻辑控制（目标内外、dev11观测值、缺截面、NaN/无穷、draft不测量）全部通过；这些是提供数值的分支控制，不能冒充重新建造几何。三项公开控制用的是四尺寸assert插入版，明确与最终文档片段区分。证据：[实际片段逻辑控制](../workspace/quality-v7-audit/legacy-final-assertion-proposal/product-guide-validation/logic-controls.json)。

曲面段落还把旧源“adapt callbacks”替换为具体迁移：完整构造/finish移入返回fresh BuildSession的函数，保留原intent的普通Python路径，局部stations同时进入外形、内腔和station_at，最终验收/export与可导入试算分开。dev11源是模块级构造且station_at读全局，因此这些是实际迁移陷阱，不是又一套solver；原有限差分方法和30次上限保持。主入口删除重复断言句，不新增入口。上述是已验证机制及迁移说明，尚未证明模型采用、完整质量通过或正式A/B改善；下一轮必须保持原开发输入与预算验证。

### dev-12：更接近固定口宽，但两例仍超时，最终要求未全部通过

`dev-12` 冻结 `2105526741e9319ed72b81f27b3d525a032816c2`，保持原两开发case、旧cup seed/intent、runtime/harness、同模型medium、每例1,200秒和并发2。杯子与外壳均timeout，实际 **1200.060 / 1200.024秒**，保留原 **05:40:28.702 / 05:40:28.695 UTC** 截止。本轮没有可见SDK/provider断流或重连错误，只有固定启动warning；这不证明不可见设施状态完全正常，也不把工具间空档当作思考证据。没有反馈模型、自动补跑或正式holdout。两例cleanup和独立session路径/cwd复核均0残留，没有发信号；杯子102文件、外壳50文件的cutoff→after-exit→current逐一相同，**0晚写、0不稳定**，冻结完整性通过。证据：[终态与过程报告](../workspace/quality-evaluation-v1/development/dev-12/inspection/development-transport-review.md)、[清理复核](../workspace/quality-evaluation-v1/development/dev-12/inspection/post-cleanup-verification.json)。

杯子 **56.765秒**首草模成功、**88.912秒**读取完整曲面指南、**154.132秒**首改源；7次草模全部成功。共 **4次completed compile，其中1失败、3PASS**，首PASS **520.773秒**，最后PASS **1122.612秒**。这是一个模型会话里的连续迭代，不是3或4个独立样本。最终轮 `0914af1f-c86a-47c5-aba8-a95c51b75c43` 的 **32个文件全部匹配cutoff**，源SHA `7390b413…`、STEP `4364f2de…`、report `ecf7b63a…` 和预览同版；历史PASS单独保留，最终交付只判断真实终态。模型在超时前没有最终回复或turn.completed，但已有同版完整工件，不能把transport timeout等同于零产物。证据：[全部完成轮与终态绑定](../workspace/quality-evaluation-v1/development/dev-12/inspection/complete-snapshot-terminal-bindings.json)。

**1168.534秒**官方测量绑定最后一次正式STEP，整体包络 **81.999989583716×62.000026054283×95.000000100000 mm**，z95外口 **54.0001013664441 mm**。相对固定54 mm的误差0.0001013664441 mm，比原 **1e−4 mm** 容差多约 **1.36644e−6 mm**，因此严格判据仍失败，不能四舍五入成通过。它明显比dev11的54.099425 mm接近目标，但这项数值改善不等于全模型合格。最后可见消息报54.0001并称尺寸与报告一致，没有明确承认该边界超差。源确实采用5 mm真实直脚extrusion，但没有加入旧合同最终尺寸guard或联合校准callback；仍手调站点和内缩量。首PASS后WALL_INSET 6→7→7.75，p05仍1.80754 mm，最终min0.00567、采样区域违反比例0.060866及两项warning保留；口/底有限截面和模型对曲率/自由口缘的归因不能证明全壁厚或实际警告原因。独立同版足缘/装配结果另记，不拿首PASS测量充当最终版。证据：[同版官方测量及精度判断](../workspace/quality-evaluation-v1/development/dev-12/inspection/cup-final-official-measurement-binding.json)。

外壳在 **32.410秒**读到新版−Y安装源，但首源前文档/API实际返回 **78,434字符**，随后还读完整连接/安装/意图/打印资料；**534.338秒**才写16.6KB源码。**548.076秒**首草模发现PCB底托与owner相隔1 mm，修复后 **602.851秒**草模成功，晚于dev11的297.226秒。区间Box已采用，不能继续归因旧起点/中心错误。intent先经历导入路径、back方向和cover thickness失败，**823.519秒**有效；首compile **876.526秒**又遇`screen-bottom-seat`未声明。模型补声明后进入不可变intent/修订证据返工，v2直到 **1090.697秒**通过。最终 **3次完整compile全失败**，后两轮依次是display与physical reference同名、`nodes[27].physicalFeatureRef must reference a non-display feature`。最后失败轮12文件匹配cutoff，但缺1项且没有完整report/最终preview交付，尚未到安装与制造验收；成功草模不代表最终装配通过。

原始7条完成compile全部由observer捕获，共11次快照；每个PASS均32文件/0gap/同版预览，失败的gap原样保留。零SDK错误不能消除已发生的source/contract/display衔接成本：外壳读到新示例仍没完成最终绑定，杯子更早达到包络并把口宽逼近精度边界，但未采用现有成品guard，之后继续围绕相同warning做完整导出循环。当前只能确认这些具体数值与路线变化，**仍不能宣称整体建模质量提高**。记录和源码/日志SHA见[本轮收据](../workspace/quality-evaluation-v1/development/dev-12/inspection/review-receipt.json)；后续机制或示例改动须另行验证，不能追认本轮成功。

### dev-12 后：按打印用途约定尺寸范围，拆开构造操作与需求绑定

用户指出普通3D打印不应围绕小数点后多位反复调整。按这一新方向，初次意图编写时，普通外形使用名义值±0.1 mm的已有 `constraint.range`，普通可编辑长度优先取0.01 mm步长，显示通常保留两位。显式用户公差与功能配合优先；范围是建模偏差约定，不是设备精度保证。原始几何/测量、内核数值精度、壁厚/底板/间隙/碰撞检查不舍入、不放宽；已绑定合同的修改仍可追溯。没有新增schema、配置层或公共API。主SKILL 108→112行，曲面指南仍119行；曲面例子只增加四个尺寸范围及一项说明，名义尺寸/截面位置/壁厚/底板和其他意图内容不变。校准先量实际几何，对允许范围内的尺寸立即停止，不继续追逐名义值；保留严格固定尺寸路线。

冻结dev12终态另做一次源参数舍入对照：只把三项可编辑长度 `81.93421/61.81177/55.24615` 保存为 `81.93/61.81/55.25`，其余源码字节相同，原intent/profile和冻结runtime不变。一次managed draft加一次实际STEP测量用时14.795秒；原始XYZ为 `81.99584459675134 / 61.99848575489486 / 95.00000010000028 mm`，z95外口为 `54.00512665990029 mm`。新普通外形±0.1范围下四项满足，最大偏差约0.0052 mm；原固定1e−4列仍记录X/Y/口宽失败。没有solver、最终compile、壁厚或整体质量通过声明。证据：[舍入对照](../workspace/quality-v7-audit/ordinary-dimension-rounding-prototype/)。先前6次精确校准的隔离原型仍保留，但不把追逐1e−5作为普通尺寸的产品要求。

`BuildSession.add/cut` 现在允许用明确合法owner记录未声明的内部构造ID，只留下真实材料操作事件，不强制向意图增加需求。每项已声明物理feature仍须同owner真实绑定；未声明observe、错误owner、重复绑定与失败材料操作继续拒绝并回滚。首次内部add记录实际初始化，不伪称union。正式scene和独立build-check都补齐所有声明物理feature检查，事件、报告观察值或同ID显示节点不能替代缺失绑定。真实cutters、finish、observe、安装和紧固件链路保留。独立审查限定：绑定完整性不证明功能或任意recipe的最终材料；旧hybrid独立审计会按当前BRep-only scene规则新拒绝。证据：[独立审查](../workspace/quality-v7-audit/construction-binding-decoupling-review/final-binding-static-review-01/review.md)。

回归去重共81个Python测试方法通过：BuildSession 19、manifest 29、scene 28、公开作者示例5；新增方法仅7个。5个公开示例完整suite用时152.303秒，覆盖实际STEP/网格、安装与四颗紧固件、曲面修改后意图不变、真正越界拒绝。曲面负例的Y控制改为80.4，使实际包络越过新±0.1范围；未删除原错误断言。另17项纯片段/意图控制通过，包含范围内只测一次即exit0、混合fixed/range、原始值越界但显示舍入相同仍拒绝、空截面/NaN和draft行为；这些不冒充真实几何测试。日志：[公开示例](../workspace/quality-v7-audit/construction-binding-decoupling-review/public-validation-01/test.log)、[manifest与scene](../workspace/quality-v7-audit/construction-binding-decoupling-review/root-validation-01/)、[片段与意图控制](../workspace/quality-v7-audit/surface-range-update/receipt.json)。

新约定不能改写历史严格判据或追认timeout。按±0.1 mm看，dev11与dev12的口宽都满足，二者小数差异不再是实质质量提升的依据。后续比较须先固定同一公差政策，在两组使用相同要求；原始结论另存，未知壁厚、缺交付和装配失败不因公差变化成为通过。尚未启动下一轮模型比较、正式holdout或读取封存内容，当前不宣称整体建模质量提高。

### 后续校准修正：越界项向名义值提议，避免停在允许边缘

范围残差原先同时用于判断合格和计算更新，可能把越界尺寸恰好推到允许边缘。纯数值耦合对照中，旧提议保存0.01步长后得到 `82.103 / 62.043 / 53.904 mm`，X再次越界；新提议只让越界项朝原名义值调整，同样保存后为 `82.004 / 62.0375 / 54.003 mm`，三项均在范围内。区间内尺寸仍不追逐名义值；一步接受仍比较原区间残差，固定尺寸精度、2 mm步长上限和30次评估上限保持不变。仅修改现有指南的提议计算并加一行说明，无新API、阈值或阶段。

5组有限数值控制通过；实际接入后的指南代码块另有6项控制通过，覆盖已合格时只量一次、耦合控制保存两位、fixed不放宽、错误平面和非有限测量。固定尺寸受耦合时，保存两位仍可能不合格，必须重测；本修正不承诺非线性构造总能一步收敛。没有运行CAD或据此宣称实际建模质量提高。证据：[反例与审查](../workspace/quality-v7-audit/dimension-range-evaluation-review/calibration-boundary-review/review.md)、[实际指南片段控制](../workspace/quality-v7-audit/dimension-range-evaluation-review/calibration-boundary-review/product-snippet-validation/validation.json)。此修正发生于dev13冻结之后，不计入其候选 `001b845` 的效果。

### dev-13：共同尺寸范围下，杯子更快完成但底脚退化

固定 baseline `2105526` / candidate `001b845`，两杯、两外壳共同采用普通外形±0.1 mm，N=4。两杯最终同版 STEP 的五项外形尺寸均通过；candidate 在836.643秒自然结束，首次PASS为661.092秒，baseline首次PASS为872.082秒并在1200.054秒超时。外壳两路均超时，0次完整compile、没有最终绑定几何。原终态、缺交付、完整分母均保留，没有SDK故障、晚写或残留进程。

同组96条足缘探针，baseline无短段，candidate有57条已知首次材料段小于2 mm。进一步复核三条实际STEP见证，确认约0.16、0.96、0.18 mm的外底脚回缩薄楔：由外底进入，在中央底板高度以下穿出外部，再经同一外部曲面进入；不是相对内外壁厚或杯口截断。其余短段及全局壁厚仍未分类。固定相机的两杯四视图可见该底脚形态差异，属于非盲观察。candidate“已消除底脚薄楔”的最终声明被实际反例否定，**本轮不能宣称整体建模质量改善**。详见[开发对比结论](../workspace/quality-evaluation-v1/development/dev-13-paired-ranges/inspection/development-quality-review.md)。

两杯各用原冻结runtime、四个原样输入，在干净目录一次公开重编译，均PASS。各自重放STEP的DATA段与原终态逐字相同，独立读回均为有效单实体，原session文件未变；这只证明同版源码可重建，不追认原超时、底脚缺陷或其他未知。见[干净重放](../workspace/quality-evaluation-v1/development/dev-13-paired-ranges/inspection/clean-replay/review.md)。正式holdout未读取或执行。

### 直接修改底层普通尺寸精度

按用户进一步要求，普通固定尺寸、范围边界及最终外截面的实际测量容差统一为 **0.01 mm**，贯穿source记录、manifest、单色/彩色QA与独立STEP检查。`a3d measure`完整JSON和摘要中的测量长度保存到两位小数，并保持两者SHA绑定；几何计算与用于验收的原始测量不受输出舍入影响。显式精密要求可在原尺寸项设置 `measurement_precision_mm`，只允许从0.01收紧到0.0001 mm；该字段参与原有目标修订检查。独立STEP的`--expect`默认容差也改为0.01，显式`--tol`仍仅调整自己的诊断比较，无法放宽合同要求。

既有0.0002 mm记录舍入补偿、0.05 mm表示一致性、几何记录自身一致性及壁厚/底板/间隙/碰撞/内核规则保留。未增加建模阶段。当前校准指南仍只调整越界项、朝名义值提议，使用各尺寸所选精度；实际代码块的6项纯数值控制通过，显式严格尺寸仍可能无法用两位控制量满足，必须重测。该改动发生于dev13冻结之后，不计入其候选效果。

三组干净公开compile实测：固定40 mm目标下，实际40.009 mm通过，40.011 mm由尺寸gate拒绝；最初声明0.0001 mm精度时，40.009 mm同样被拒绝。各一次compile及raw STEP测量合计25.709秒，无重试；失败保留为诊断工件，通过案例仍有原有QA warnings及待视觉检查状态，不当作成品质量证明。见[完整编译对照](../workspace/quality-v7-audit/measurement-precision-review/public-compile-validation/results.md)。

最终去重96个Python方法通过：manifest 29、intent revisions 15、BRep包络1、测量17、capability 12、公开作者示例5、color pipeline 17；新增方法仅2个。公开示例完整回归152.593秒；实际STEP覆盖默认/显式精度、fixed/range、独立`--expect`、`--tol`不能放宽合同、原始测量与两位输出一致绑定。彩色QA另用初始目标20.009 mm、实际宽20 mm验证默认精度。补跑发现前次全feature绑定规则下两份旧color fixture漏绑已观察实体，现补上真实detail/cutter/region绑定，原几何与断言保留；两个失败尝试和最早系统Python缺NumPy日志均保留，不算通过。只对最后独立tower方法重跑，没有把重复执行累计为新测试。证据：[最终精度验证记录](../workspace/quality-v7-audit/measurement-precision-review/result.md)。

### 可运行的足部构造与共同装配基准

曲面示例在共同内腔切除之前，将首截面的正向挤出融合进外 loft；`FOOT_HEIGHT=4` 是独立构造长度，0 可关闭，不与底板厚度混用。主 SKILL 不增加要求，原指南中的抽象足部说明改为实际示例中的两行调用。默认 ruled 示例的新旧实际 STEP 双向差集、体积差和包络差均为0。

对 dev13 candidate 的隔离原样重放，只加入4 mm真实脚柱，原合同、打印配置和其他源参数保持。沿用原先冻结的96个物理射线位置，本次新旧 STEP 的已确认短段从55个降到0个，最短首次连续材料段从约0.16增至4 mm；三处已知反例的材料段由约0.16/0.96/0.18变为4/12.82/4 mm，五条中央底板仍为3.5 mm，五项外形仍在原±0.1范围。原始57个短段中2个分段状态一直是unresolved，55是cross-checked口径；逐项分段状态没有变化，旧未知未被追认。该结果证明固定探针覆盖处的足缘改善，不证明全壁厚、模型自行采用或总体任务成功率；这些是公开draft的真实STEP，未充当完整compile交付。证据：[构造对照原始测量](../workspace/quality-v7-audit/foot-construction-update/measurements.json)。

安装示例将独立定位宽/高改成空腔区间加单个材料边距，将独立螺丝X/Z位置改为距外壳角部的共同边距。默认定位54×34、轴偏移33/23保持，独立需求不随构造参数自动改写。这个厚框示例不能直接保证薄壁外壳安全：切刀须落在真实接收材料里，同时满足规定最小剩余壁厚。dev13原外壳的两个定位切刀均落在空腔里，缩小后离实体更远；新示例针对这一参数失配，不宣称已完成双模块外壳。证据：[失败与参数审查](../workspace/quality-v7-audit/enclosure-starter-parameter-review/review.md)。

安装默认例子另用两次公开draft和真实STEP核对，frame、cover及独立参考module逐一配对，三对双向差集均0 mm³（7.573秒）；参考模块不计作制造件。公开作者测试最终去重6个方法通过，其中新增1个86×18×66外形/54×34×5模块的真实参数变体，检查两个制造实体、定位槽移除材料、四螺丝及安装审计。固定旧54×34定位尺寸的负例仍以零移除量被拒绝。完整suite182.193秒，原5方法通过；新增方法首次仅因错误文字断言失败，改查实际结构化SOURCE.CUT_MISSED_OWNER后单独复跑27.018秒通过，产品代码未因该断言调整。详见[公开回归与完整失败记录](../workspace/quality-v7-audit/construction-example-validation/result.md)及[默认几何等价](../workspace/quality-v7-audit/enclosure-starter-parameter-review/default-geometry-equivalence/results.md)。这些结果尚不能代替相同输入/预算的模型比较。

交叉只读审查未发现产品阻断；补强现有测试helper，从首次intent生成到bound draft和完整compile后核对同一SHA。参数变体单方法再次通过（28.026秒），确认正例构造不回写初始独立目标；仍为6个独立方法，不把复跑累计成新增测试。

### 尺寸失败提示直接指向名义值

在dev14两杯首次完整compile中，typed顶口分别约54.12和54.11 mm，均仍在上边界之外；现有泛用QA提示没有直接说明应朝名义54 mm修正。当前worktree仅在`_issue`增加6行选择已有typed STEP外宽/深失败，替换为朝声明名义值调整构造控制、raw STEP检查通过即停止该尺寸校准的151字符静态动作提示。无新helper/API、重复数字、舍入建议或SKILL规则；严格精度同样适用，显式hint优先，壁厚/安装/接口等原提示保持。所有原expected/observed、原始精度和判定均不变。

扩展1个现有pure摘要无修改方法，覆盖width/depth、普通/严格精度、显式hint及非typed检查等8个子情景，0.010秒通过；子情景不计为新增方法。另将dev14两份实际失败JSON逐项重送issue/summary，尺寸提示替换、薄区提示保持、全部原始expected/observed和冻结输入字节不变，原FAIL未改。没有CAD或新模型调用，不能据此声称模型已减少返工。此项发生在dev14启动后，只在当前worktree；该轮冻结候选c0511cc及两臂运行目录不包含此提示，不能把其效果算入dev14。证据：[实际错误JSON复用](../workspace/quality-v7-audit/dimension-repair-hint-proposal/product-validation/actual-error-replay.json)、[纯摘要测试](../workspace/quality-v7-audit/dimension-repair-hint-proposal/product-validation/summary-test.log)。

有限交叉阅读另发现authoring-example的旧“record the range before modeling”容易被理解为草模前必须写合同，现仅改成首次intent中、调整该目标之前声明范围；已有目标及范围修订约束保持，未新增流程。其他四份相关公开指南未发现明确的draft-first冲突，不能据此认定dev14准备空档的原因。该文字澄清同样不属于已冻结的dev14干预。

### dev-14：局部足缘缺陷未复现，整体胜出仍未证实

固定baseline `0c4b015` / candidate `c0511cc`，相同原杯子/外壳输入，N=4、每例1200秒、相同model/medium及并发2；普通外形±0.1与底层0.01精度为共同条件。candidate杯子1000.63秒自然结束，baseline1200.12秒超时但有完整终态成品；两外壳均超时，无草模/compile/STEP。保留全N4，没有反馈、延长预算、补跑或读取holdout。详见[本轮完整结论](../workspace/quality-evaluation-v1/development/dev-14-paired-foot-installation/inspection/development-quality-review.md)。

两杯最后实际同版STEP均为有效单实体，5项外形在原±0.1范围内，各96条足缘全cross_checked、0短段/0unknown，5条中央底板线均3.5 mm。各840个法向诊断没有检出小于2 mm的弦，但首次出口未独立证明完整，所有全局壁厚/未覆盖薄区仍unknown。无符合预定规则的短足点，不额外启动3点分类。candidate没有复现dev13的足缘采样缺陷，但baseline这轮也自行用了5 mm实体脚并通过同项，因此不能将采用或结果归为候选独有改善。首PASS提前39.05秒也只是单次配对的过程差异，不是质量胜出。

两杯原样源码各在独立空目录、原冻结runtime下公开compile一次，60.41 / 62.93秒均通过。各自原件与重建STEP DATA逐字相同，实际读回valid/solid/bbox/volume一致，原会话68/73文件和四输入SHA未变；未作对齐、缩放或布尔差集。两外壳无完整锚点/输入，不由评估者补写重建。见[原样重建](../workspace/quality-evaluation-v1/development/dev-14-paired-foot-installation/inspection/clean-replay/review.md)。

外壳baseline直到1139.78秒才写首版源码，candidate只有profile；两路实际文档/API读取复合输出约6.9万/8.9万字符，但不能单凭字符或工具间隔认定超时原因。可见目录枚举权限错误与缺少工具返回证实的后续工作目录错误自述分别记录；没有可见SDK断流，亦不据此断言工具都正常。四路终态→退出→当前库存一致，0晚写、无残留，完整冻结检查通过。见[可见工具事件](../workspace/quality-evaluation-v1/development/dev-14-paired-foot-installation/inspection/development-transport-review.md)和[外壳逐项证据缺口](../workspace/quality-evaluation-v1/development/dev-14-paired-foot-installation/inspection/enclosure-terminal-assessment.md)。

保留已用因果控制证实的足部构造及装配共同基准改进；没有把主SKILL继续加长。当前可明确宣称的质量改善仍限于原样旧杯源码只改实体脚后，固定96位置的确认短段55→0、最短段约0.16→4 mm且尺寸/中央底板保持。本轮结果不足以宣称整体建模成功率提高；主要剩余缺口是外壳及时形成可验收几何及完整壁厚/功能证据。

固定中性四视图完成，两杯各4图、两外壳unavailable，完整名单保持N4。两个未获得版本映射且互不共享上下文的审查代理分别实际查看9张图，都认为主要可见形态通过、两者外观相当，同时指出共同的短直立脚带偏离参考图直接落底的表现；不把有限可见形态通过升级为完整平顺或制造质量通过。两次原样重建、独立有限QA、固定渲染及匿名视觉复核均已收束，最终完整性与30项评价方法复核通过，8个相关精确路径无匹配进程。

### 将螺丝构造与记录收进同一个调用

现有`SelfTappingScrewPair`新增`bind`方法：同一个刚性Location放置通孔刀、盲孔刀和螺丝柱，并从这次构造的参数及变换生成原scene fastener记录。显式选择`boss_mode="add"`或`"observe"`，三个实际操作经原`BuildSession.capture()`整组提交/回滚。不会在零新增失败后偷偷改成observe，不自动写需求，不把元数据代替实物验收。沉头的孔径、深度与最小槽底材料约束一起保留，minimumBossWall仍取原要求而非实际测得的较大值。原9个recipe函数的构造逻辑和函数参数保持，仅给螺丝pair的evidence补存两个原构造输入。静态终审见[绑定审查](../workspace/quality-v8-audit/screw-binding-review.md)。

安装示例去掉三次手动变换和重复的数字/轴线字典，源码165→157行、减少906字节；后台统一实现增加53行，按符号查询即可读取方法签名。主SKILL将既有draft命令移入cp块，先看可运行示例再在同一源码上适配需求，行数115保持、减少40字节；authoring说明增加151字节解释这一调用。独立需求生成器、场景/完整feature gate和最终安装/螺丝几何检查未改，未增加新schema、阶段或参数文件。

相关回归去重29个方法通过：recipe14（新增2）、capability12、公开作者3。新测试用非原点三轴旋转、M4非默认孔/柱尺寸、沉头和实际BRep材料/体积，验证轴与几何相符；覆盖原pair不变、已有柱observe、零新增add拒绝、最后通孔cut错位时两实体/feature/evidence全部回滚。公开作者3方法55.507秒通过，包含默认实物装配、改尺寸定位与螺丝正负例、导入不造几何且目标独立。首次运行均通过。日志见[几何绑定回归](../workspace/quality-v8-audit/screw-binding-validation/report.md)、[公开作者回归](../workspace/quality-v8-audit/public-authoring-regression.log)。stock skill validator因managed venv缺PyYAML未运行成功，未安装依赖；frontmatter与原字节相同、11个引用目标存在、diff格式检查通过。

另拒绝一个看似简化杯脚的方案：在dev14C同一终态源码中，仅将外loft站点从z≥5改回完整STATIONS，保留5 mm实体脚和其他参数。两次草模及同96+5物理位置对照均完成，但顶口54.03→53.46 mm明确越界，且固定八图仍有短脚带，没有明确平顺收益。原脚材料差集体积0、脚和中央底板采样不变，不通过删脚掩盖问题；没有补调、完整compile或新增模型样本，也未将此改法加入产品或新指南规则。见[单变量对照](../workspace/quality-v8-audit/foot-transition-control/review.md)。

当前skill另做一次独立前向可用性试做：新代理无旧评测/版本信息，按92×74×24 mm两件式显示模块框的独立需求建模，实际采用四处pair.bind。约1分钟看见原始示例草模、约2分35秒完成需求适配草模；完整compile仅1次、20.44秒，输出两实体STEP/STL/3MF，编译状态为awaiting-visual-review（deliveryReady=false）。安装/螺丝审计通过，正式预览及STEP内部视图均实际查看；另有29项最终STEP检查通过，包含完整54×36光学区、模块/后盖连续插入扫掠、重力支撑、侧向定位、止退及螺丝刀通路。这29项成品检查不并入前述29个回归方法。检查脚本曾因空交集None处理中断，补齐后完成，没有隐藏或计为编译失败。

此试做仍未完整满足交付：定位圈自由端同一处局部厚度条件在盖板/排盘形成两条警告，完整3 mm环壁材料包含检查不能替代全局最小壁厚证明；24 mm只验证了制造件包络，外部盘头螺丝使含硬件总深超出。警告、原合同和正式compile结果均原样保留，未将pass=true升级为整体合格。该代理与先前dev14的模型/任务条件不同，也不是配对A/B，不能据此声称成功率或整体建模质量提高。可确认的本轮收益是减少螺丝几何/记录的重复输入且真实改尺寸用例可运行；全局壁厚与硬件包络仍是后续问题。见[试做报告](../workspace/quality-v8-audit/forward-enclosure/RESULT.md)及[本轮结果](../workspace/quality-v8-audit/result.md)。

### 通用参数关系修复，不按上一轮模型特调

用户要求继续优化且避免特调。本轮先只读审查并固定反例，保留三个有可复现原因的小改动；未修改旧杯/外壳参数、目标、精度或既有验收阈值。主SKILL字节完全不变（115行），未增加阶段、schema或包装API。首次add的既有行为差异暂缓，避免将可能合法的多实体构造一并禁止。

螺丝长度：原建议最大头下长度=盖厚−沉孔深+啮合+孔底间隙，会将最后的预留空隙全部吃掉。保留现有min/max字段，两者均改成固定几何下的计算长度=盖厚−沉孔深+啮合，刀具/柱体形状不变。三套独立尺寸、有/无沉孔，分别轴向与离原点三轴旋转，用真实盖体承压面和盲孔底部核对；旧版6个配置均在最大长度处失败，修复后通过。对应recipe suite 15方法（新增1）通过。同步修正指南和示例假设，不建议通过向上选长度消耗间隙；不同供应商长度需显式重设计并复验。

盖厚：删除P.fastening内导入时生成的重复快照，造孔和首次生成接口元数据均读取当前P.cover_thickness。固定2.5/4.0 mm两个改型，参数初始化后仅改这一主值，整体80×16×60和模块50×30×5的独立要求不变。旧版2.5 mm被配合审计拒绝，4.0 mm虽然编译通过但记录仍为3.0 mm；修复后两者真实STEP厚度、四孔贯通、scene/intent盖厚一致。新增1方法和原3方法全部通过；旧2次+新4次完整compile，共6次。首次旧版测试误读intent顶层键发生KeyError，已保留原日志并对保存产物纠正核对，未记为产品缺陷或额外重编译。frame/plate原局部薄壁采样告警仍保留，不把上述局部一致性通过升级为全要求合格。

沉孔：原检查只核对历史刀具体积/包围盒与最终小通孔，沉孔被后续添料堵回仍可能放行。新增已有轴/直径/深度定义的最终沉孔开放体积探针，沿用现有布尔、内缩和容差，报告记录实际重叠体积。固定两套尺寸、矩形/圆盖、轴向/三轴旋转、完整/局部回填/变浅及正常/无沉孔，共20个配置：旧12个阻塞反例全部误放，新版全部拒绝，8个正常对照保持通过；前后测试SHA相同、旧检查结果逐项相同。该suite 7方法（新增1）及相邻interface geometry 12方法通过。另将真实旋转M4配方经BuildSession和管理网格转换送入新检查，14项物理检查通过（单独集成核对，不另计回归方法）。

总计38个不重复回归方法通过，新增3个；本轮可确认的是错误长度建议、陈旧派生参数和沉孔漏验减少，不能把更严检查或固定变体通过当作整体生成质量的A/B结论。独立静态复核未发现特定产品条件分支。原始反例、修复后几何和日志见[本轮结果](../workspace/quality-v9-audit/result.md)。
